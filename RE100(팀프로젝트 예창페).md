
테이블 구성 

    __tablename__ = "optimization_results"

    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
    timestamp = Column(DateTime, nullable=False, index=True)
    region_code = Column(String(10), nullable=True)

    # 의사결정 결과
    action = Column(String(20), nullable=False)  # "PRODUCE_H2", "SELL_POWER", "IDLE"
    expected_profit = Column(Float, nullable=False)  # 예상 수익 (원)

    # 계산된 값
    usable_mw = Column(Float, nullable=True)  # 사용 가능한 전력 (MW)
    h2_production_kg = Column(Float, nullable=True)  # 수소 생산량 (kg)
    carbon_reduction_kg = Column(Float, nullable=True)  # 탄소 감축량 (kg)

    # 수익 구성
    h2_sales_revenue = Column(Float, nullable=True)  # 수소 판매 매출
    carbon_value = Column(Float, nullable=True)  # 탄소 가치
    subsidy_revenue = Column(Float, nullable=True)  # 보조금
    power_sell_revenue = Column(Float, nullable=True)  # 전력 판매 수익
    total_cost = Column(Float, nullable=True)  # 총 비용

    # 입력 데이터 (참고용)
    smp_price = Column(Float, nullable=True)  # 계통한계가격 (원/kWh)
    h2_price = Column(Float, nullable=True)  # 수소 판매가격 (원/kg)
    surplus_mw = Column(Float, nullable=True)  # 잉여전력 (MW)
    carbon_credit_price = Column(Float, nullable=True)  # 탄소배출권 가격 (원/tCO2eq)

,


    __tablename__ = "environmental_indicators"

    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
    timestamp = Column(DateTime, nullable=False, index=True)

    # 탄소 시장 지표 (시계열)
    carbon_credit_price_ton = Column(Float, nullable=False)  # KAU 가격 (원/tCO2eq)

,

    __tablename__ = "environmental_configs"

    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
    config_name = Column(String(50), nullable=False, unique=True)  # 예: "default_korea"

    # 탄소 감축 지표 (벤치마크 데이터)
    benchmark_emission_factor = Column(Float, nullable=False, default=9.1)  # kg

    # 청정수소 인센티브
    green_h2_subsidy = Column(Float, nullable=True, default=2500.0)  # 원/kg

    # 그리드 탄소 집약도
    grid_emission_factor = Column(Float, nullable=True, default=0.45)  # kg-CO2/kWh

    # 메타 정보
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    description = Column(String(200), nullable=True)

,


    __tablename__ = "h2_market_indicators"

    # 기본 필드
    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
    timestamp = Column(DateTime, nullable=False, index=True)
    region_code = Column(String(10), nullable=True)

    # 외부에서 가져오는 시장 가격 (API/크롤링)
    smp_price = Column(Float, nullable=True)  # 계통한계가격 (원/kWh)
    h2_price = Column(Float, nullable=True)  # 수소 판매가격 (원/kg)
   
,


    __tablename__ = "h2_system_configs"

    # 기본 필드
    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
    config_name = Column(String(50), nullable=False, unique=True)  # 예: "jeju_demo_plant"
    region_code = Column(String(10), nullable=False)

    # 설비 사양 (카탈로그 스펙)
    system_limit_mw = Column(Float, nullable=False, default=3.3)  # 설비 최대 용량 (MW)
    kg_per_mwh = Column(Float, nullable=False, default=18.5)  # MWh당 수소 생산량 (kg/MWh)
    efficiency_pct = Column(Float, nullable=False, default=95.0)  # 가동 효율 (%)

    # 운영 비용
    water_cost_per_kg = Column(Float, nullable=False, default=100.0)  # 물 비용 (원/kg)
    maintenance_cost_per_hour = Column(Float, nullable=True, default=0.0)  # 유지보수 비용 (원/시간)
    startup_cost = Column(Float, nullable=True, default=50000.0)  # 기동 비용 (원)

    # 저장소 사양
    storage_limit_kg = Column(Float, nullable=True, default=10000.0)  # 저장 한계 (kg)

    # 메타 정보
    is_active = Column(Boolean, default=True)  # 현재 사용 중인 설정인지
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    description = Column(String(200), nullable=True)  # 설정 설명


,


    __tablename__ = "energy_status"

    # 기본 필드
    id = Column(Integer, primary_key=True, index=True, autoincrement=True)  # serial (report_id와 동일)
    timestamp = Column(DateTime, nullable=False)  # timestamp (데이터 생성 시간)

    # 지역 정보
    region = Column(String(100), nullable=True)  # 지역명 (예: '경기도 안산시')
    region_code = Column(String(10), nullable=True)  # varchar(10) (지역 코드, 기존 호환성 유지)

    # 에너지 타입
    energy_type = Column(String(20), nullable=True)  # "solar" 또는 "wind"
    
    # 설비 및 발전 정보
    installed_capacity_mw = Column(Float, nullable=True)  # 설비 용량 (MW)
    expected_generation_mw = Column(Float, nullable=True)  # 예상 발전량 (MW)
    efficiency = Column(Float, nullable=True)  # 효율 (%)

    # 수요 정보
    current_demand_mw = Column(Float, nullable=True)  # 현재 전력 수요 (MW)
    demand_source = Column(String(50), nullable=True)  # "realtime_api", "parameter", "parameter_or_default"

    # 잉여 전력 정보
    surplus_mw = Column(Float, nullable=True)  # 잉여 전력량 (MW) - surplus_power_mw와 동일
    surplus_power_mw = Column(Float, nullable=True)  # 잉여 전력량 (MW) - 기존 호환성 유지
    surplus_power_kwh = Column(Float, nullable=True)  # 잉여 에너지량 (kWh)
    surplus_duration_hours = Column(Float, nullable=True)  # 잉여 지속 예상 시간(시간)
    is_curtailment_risk = Column(Boolean, nullable=True)  # 잉여 전력 존재 여부


,


    __tablename__ = "solar_power"

    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
    region = Column(String(100), nullable=False, comment="세부지역")
    energy_source = Column(String(50), nullable=False, comment="발전원")
    province = Column(String(50), nullable=False, comment="광역지역")
    capacity_mw = Column(Float, nullable=False, comment="설비용량(MW)")

,


    __tablename__ = "wind_power"

    id = Column(Integer, primary_key=True, index=True, autoincrement=True)
    region = Column(String(100), nullable=False, comment="세부지역")
    energy_source = Column(String(50), nullable=False, comment="발전원")
    province = Column(String(50), nullable=False, comment="광역지역")
    capacity_mw = Column(Float, nullable=False, comment="설비용량(MW)")


데이터 출처 

공공테이터 api
![[Pasted image 20260126124856.png]]
4개 

hying 수소유통정보시스템

https://www.h2nbiz.or.kr/sym/mnu/mpm/EgovMainMenuIndex.do
지역마다 수소가격 크롤링

각종 보고서 pdf파일
https://www.gir.go.kr/home/board/read.do?menuId=36&boardId=82&boardId=82&boardMasterId=2



operating_cost_per_ton



power_assist_kwh_per_ton
https://www.sciencedirect.com/science/article/abs/pii/S0960852413001739
https://www.css.cornell.edu/faculty/lehmann/publ/ES&T%20published%20online,%202009,%20Roberts.pdf


carbon_removal_factor (tCO₂e/톤
https://koreascience.or.kr/article/JAKO201406234294421.pdf
https://koreascience.kr/article/JAKO202129857833059.pdf
https://www.kjssf.org/media/sites/ksssf/2024-057-02/N0230570207/N0230570207.pdf
표준값 활용 IPCC 가이드라인
https://www.ipcc-nggip.iges.or.jp/public/2019rf/pdf/4_Volume4/19R_V4_Ch02_Ap4_Biochar.pdf



conversion_rate_pct (바이오차 수율 %)
https://koreascience.or.kr/article/JAKO201120842651540.page
https://koreascience.or.kr/article/JAKO202218852197806.page
https://koreascience.or.kr/article/JAKO201811648109966.pdf
https://koreascience.or.kr/article/JAKO202213650119312.pdf
감귤
https://www.ktappi.kr/media/sites/ktappi/2025-057-03/N0460570305/N0460570305.pdf


round_trip_efficiency_pct
https://cdn.standards.iteh.ai/samples/120618/518b9e4c39ad446291592d6aa5c352ab/IEC-TR-62933-2-201-2024.pdf
https://journal.kpvs.or.kr/media/sites/cpr/2023-011-02/N0170110205/N0170110205.pdf
https://www.samsungsdi.com/upload/ess_brochure/201902_Samsung%20SDI%20ESS_EN.pdf



### 1. BiocharSystemConfig 모델 확장

- 공정 조건 필드 추가:

- biomass_type: 원료 종류 (forestry, agricultural, food-waste, livestock, seaweed, citrus_pomace)

- pyrolysis_temperature: 탄화 온도 (°C)

- residence_time_min: 체류 시간 (분)

- heating_rate: 가열 속도 (°C/min)

- moisture_content_pct: 수분 함량 (%)

- use_calculated_rate: 계산 모드 선택 (True: 논문 기반, False: 고정값)

### 2. 전환율 계산 함수 생성

파일: app/domain/biochar.seoeunjin.com/services/biochar_conversion_calculator.py

- 원료별 기본 수율 (논문 데이터 기반):

- 산림 부산물: 40%

- 농업 부산물: 32%

- 음식물 쓰레기: 35%

- 가축분뇨: 30%

- 해조류: 28%

- 감귤박: 25%

- 공정 조건 보정:

- 온도 보정: 500°C 기준, 높을수록 수율 감소

- 체류 시간 보정: 30분 기준, 시간 증가 시 수율 감소

- 가열 속도 보정: 10°C/min 기준

- 수분 보정: 수분 함량이 높을수록 수율 감소

### 3. 최적화 에이전트 통합

- h2_optimization_agent.py에서 논문 기반 계산 함수 사용

- 계산 실패 시 고정값으로 폴백

## 정확도 향상

| 방법           | 정확도    | 설명            |
| ------------ | ------ | ------------- |
| 기존 (고정값 30%) | 60-70% | 원료/조건 무시      |
| 논문 기반 계산     | 75-85% | 원료/온도/시간 등 반영 |
| 실제 플랜트 데이터   | 90-95% | 실제 운영 값       |




기술
## 1. optimization_results (AI 도메인)

목적: AI 에이전트의 최적화 의사결정 결과 저장

주요 필드:

- action: 최종 결정 액션 ("PRODUCE_H2", "SELL_POWER", "PRODUCE_BIOCHAR", "STORE_BATTERY", "IDLE")
- expected_profit: 예상 수익 (원)
- decision_*: 4가지 옵션 분배 비율 (0.0-1.0)
- score_*: 각 옵션의 점수
- value_*: 각 옵션의 가치 (원)
- carbon_impact_*: 각 옵션의 탄소 영향 (kg CO₂)
- h2_production_kg, biochar_output_ton, storage_kwh: 실제 생산량/저장량
- previous_action, last_startup_time, current_runtime_hours: 시스템 상태 추적

사용: 최적화 결과 저장 및 이전 결과 조회

---

## 2. biochar_system_configs (Biochar 도메인)

목적: 바이오차 생산 시스템의 설비 스펙 및 운영 설정 저장

주요 필드:

- conversion_rate_pct: 바이오차 전환율 (%, 기본값 30.0)
- carbon_removal_factor: 탄소 제거 계수 (tCO₂/ton, 기본값 2.18)
- power_assist_kwh_per_ton: 전력 보조 투입량 (kWh/ton, 기본값 50.0)
- biomass_type: 원료 종류 ('forestry', 'agricultural', 'food-waste' 등)
- pyrolysis_temperature: 탄화 온도 (°C, 기본값 500.0)
- biomass_capacity_ton_per_day: 일일 처리 용량 (ton/day)
- biomass_inventory_ton: 바이오매스 재고량 (ton)
- use_calculated_rate: 논문 기반 계산 사용 여부 (Boolean)

사용: 바이오차 생산량 및 탄소 제거량 계산

---

## 3. environmental_indicators (Biochar 도메인)

목적: 탄소 시장 지표의 시계열 데이터 저장 (매일 변하는 시장 가격)

주요 필드:

- timestamp: 데이터 시점 (DateTime, 인덱스)
- carbon_credit_price_ton: KAU 가격 (원/tCO₂eq)

사용: 탄소 크레딧 가격 조회 (바이오차 옵션 수익 계산)

---

## 4. environmental_configs (Biochar 도메인)

목적: 환경 지표 설정 저장 (거의 변하지 않는 상수값)

주요 필드:

- benchmark_emission_factor: 벤치마크 배출 계수 (kg CO₂, 기본값 9.1)
- green_h2_subsidy: 청정수소 인센티브 (원/kg, 기본값 2500.0)
- grid_emission_factor: 그리드 탄소 집약도 (kg-CO₂/kWh, 기본값 0.45)

사용: 탄소 감축량 계산 및 인센티브 적용

---

## 5. h2_system_configs (Green H2 도메인)

목적: 수소 생산 시스템의 설비 카탈로그 스펙 저장 (거의 변하지 않는 상수값)

주요 필드:

- system_limit_mw: 설비 최대 용량 (MW, 기본값 3.3)
- kg_per_mwh: MWh당 수소 생산량 (kg/MWh, 기본값 18.5)
- efficiency_pct: 가동 효율 (%, 기본값 95.0)
- power_conversion_efficiency: 전력 변환 효율 (AC→DC, 기본값 0.95)
- water_cost_per_kg: 물 비용 (원/kg, 기본값 100.0)
- maintenance_cost_per_hour: 유지보수 비용 (원/시간)
- startup_cost: 기동 비용 (원, 기본값 50000.0)
- storage_limit_kg: 저장 한계 (kg, 기본값 10000.0)

사용: 수소 생산량 및 비용 계산

---

## 6. h2_market_indicators (Green H2 도메인)

목적: 수소 시장 지표의 시계열 데이터 저장 (매 시간 변하는 시장 가격 및 환경 데이터)

주요 필드:

- timestamp: 데이터 시점 (DateTime, 인덱스)
- region_code: 지역 코드
- smp_price: 계통한계가격 (원/kWh)
- h2_price: 수소 판매가격 (원/kg)

사용: 수소 생산 수익성 계산

---

## 7. battery_system_configs (RE100 도메인)

목적: 배터리 저장 시스템의 설비 스펙 및 운영 설정 저장

주요 필드:

- capacity_kwh: 배터리 용량 (kWh, 기본값 1000.0)
- round_trip_efficiency_pct: 왕복 효율 (%, 기본값 85.0)
- max_charge_rate_kw: 최대 충전 속도 (kW, 기본값 500.0)
- max_discharge_rate_kw: 최대 방전 속도 (kW, 기본값 500.0)
- battery_type: 배터리 기술 ('lithium_ion', 'flow', 'lead_acid' 등)
- current_soc_pct: 현재 충전 상태 (%, 기본값 60.0)
- cycle_degradation_cost: 사이클당 비용 (원/사이클, 기본값 100.0)
- use_calculated_efficiency: 논문 기반 효율 계산 사용 여부

사용: 배터리 충방전 효율 및 비용 계산

---

## 8. battery_status (RE100 도메인)

목적: 배터리 현재 상태의 시간별 변화 추적

주요 필드:

- region_code: 지역 코드 (인덱스)
- soc_pct: 충전 상태 (%)
- available_capacity_kwh: 사용 가능한 용량 (kWh)
- expected_future_smp: 예상 미래 SMP (원/kWh)
- forecast_hours: 예측 시간 (시간, 기본값 6)
- timestamp: 데이터 시점 (DateTime, 인덱스)

사용: 배터리 충방전 결정 및 상태 업데이트

---

## 9. energy_status (RE100 도메인)

목적: 재생에너지 리포트 저장 (API 응답 JSON 구조에 맞춘 스키마)

주요 필드:

- timestamp: 데이터 생성 시간
- region, region_code: 지역 정보
- energy_type: 에너지 타입 ("solar" 또는 "wind")
- installed_capacity_mw: 설비 용량 (MW)
- expected_generation_mw: 예상 발전량 (MW)
- current_demand_mw: 현재 전력 수요 (MW)
- surplus_mw, surplus_power_mw: 잉여 전력량 (MW)
- surplus_duration_hours: 잉여 지속 예상 시간(시간)
- is_curtailment_risk: 잉여 전력 존재 여부

사용: 잉여전력 계산 및 최적화 입력 데이터 제공

---

## 10. solar_power (RE100 도메인)

목적: 태양광 발전 설비 용량 데이터 저장

주요 필드:

- region: 세부지역
- energy_source: 발전원
- province: 광역지역
- capacity_mw: 설비용량 (MW)

사용: 설비 용량 조회 (잉여전력 계산 시 사용)

---

## 11. wind_power (RE100 도메인)

목적: 풍력 발전 설비 용량 데이터 저장

주요 필드:

- region: 세부지역
- energy_source: 발전원
- province: 광역지역
- capacity_mw: 설비용량 (MW)

사용: 설비 용량 조회 (잉여전력 계산 시 사용)

프론트

next.js
react
typescript

### UI 라이브러리

- Radix UI

- 접근성 중심 컴포넌트

- 사용: Accordion, Dialog, Dropdown, Select, Tabs 등

- Tailwind CSS 3.4.1

- 유틸리티 기반 스타일링

- 커스텀 테마

- Lucide React

- 아이콘

- Recharts 2.15.4

- 차트 (에너지 데이터 시각화)


### 상태 관리

- Zustand 5.0.8

- 전역 상태 관리

- 사용: energyStore, hydrogenStore, biocharStore, optimizationStore

- React Query (@tanstack/react-query 5.90.11)

- 서버 상태 관리 및 캐싱

- 자동 리페칭, 에러 처리

### 폼 관리

- React Hook Form 7.66.1

- 폼 상태 관리

- Zod 3.25.76

- 스키마 검증

- @hookform/resolvers

- React Hook Form + Zod 통합


백

- FastAPI 0.115.0+

- 비동기 API 서버

- 자동 OpenAPI 문서 생성 (/docs)

- Pydantic 기반 데이터 검증

- Uvicorn 0.32.0+

- ASGI 서버

- 멀티 워커 지원 (프로덕션: 2 workers)

### 데이터베이스

- SQLAlchemy 2.0.23+

- ORM

- 도메인별 Base 분리 (ai_bases, greenh2_bases, biochar_bases, re100_bases)

- PostgreSQL (NeonDB)

- 서버리스 PostgreSQL

- 연결 풀링

- Alembic 1.13.0+

- 마이그레이션 관리

- psycopg2-binary 2.9.9+

- PostgreSQL 드라이버






### 1. IFRS Sustainability Standards

- **IFRS S2 Climate-related Disclosures** (2023년 발행, 기후 관련 공시 기준서) → **직접 PDF**: [https://www.ifrs.org/content/dam/ifrs/publications/pdf-standards-issb/english/2023/issued/part-a/issb-2023-a-ifrs-s2-climate-related-disclosures.pdf](https://www.ifrs.org/content/dam/ifrs/publications/pdf-standards-issb/english/2023/issued/part-a/issb-2023-a-ifrs-s2-climate-related-disclosures.pdf)
- **IFRS S1 General Requirements for Disclosure of Sustainability-related Financial Information** → **직접 PDF**: [https://www.ifrs.org/content/dam/ifrs/publications/pdf-standards-issb/english/2023/issued/part-a/issb-2023-a-ifrs-s1-general-requirements.pdf](https://www.ifrs.org/content/dam/ifrs/publications/pdf-standards-issb/english/2023/issued/part-a/issb-2023-a-ifrs-s1-general-requirements.pdf)

### 2. GRI Standards

- **GRI 305: Emissions 2016** (온실가스 배출 관련 핵심 표준) → **직접 PDF**: [https://www.globalreporting.org/pdf.ashx?id=12510](https://www.globalreporting.org/pdf.ashx?id=12510)
- GRI 전체 Standards 다운로드 페이지 (English): [https://www.globalreporting.org/how-to-use-the-gri-standards/gri-standards-english-language/](https://www.globalreporting.org/how-to-use-the-gri-standards/gri-standards-english-language/)

### 3. TCFD Recommendations

- **TCFD Recommendations Report** (2017년 본 보고서, 가장 많이 참조되는 버전) → **직접 PDF**: [https://assets.bbhub.io/company/sites/60/2021/10/FINAL-2017-TCFD-Report.pdf](https://assets.bbhub.io/company/sites/60/2021/10/FINAL-2017-TCFD-Report.pdf)
- TCFD 2021 Annex (Implementing Guidance) → [https://assets.bbhub.io/company/sites/60/2021/07/2021-TCFD-Implementing_Guidance.pdf](https://assets.bbhub.io/company/sites/60/2021/07/2021-TCFD-Implementing_Guidance.pdf)

### 4. SASB Standards (현재 IFRS 산하)

SASB은 **77개 산업별 개별 표준**으로 구성되어 있어 단일 PDF 파일이 없습니다. → **최신 SASB Standards 전체 다운로드** (PDF 컬렉션): [https://navigator.sasb.ifrs.org/](https://navigator.sasb.ifrs.org/) (무료 회원가입 후 PDF 다운로드 가능)

- SASB Conceptual Framework (개요) → [https://sasb.ifrs.org/wp-content/uploads/2019/05/SASB-Conceptual-Framework.pdf](https://sasb.ifrs.org/wp-content/uploads/2019/05/SASB-Conceptual-Framework.pdf)

**추천 사용 순서**:

1. IFRS S2 (기후 리스크·기회·metrics 중심) → 가장 중요
2. GRI 305 (Scope 1/2/3 배출량 정의)
3. TCFD Report (전략·리스크 관리 프레임워크)
4. SASB Navigator (산업별 metrics)

필요한 PDF가 더 있거나 (예: GRI 302 Energy, SASB 특정 산업 등), 특정 페이지(예: Scope 1 emissions 관련 조항)만 추출해달라고 하시면 추가로 도와드릴게요!





Supervisor (오케스트레이터) 데이터 출처
Supervisor는 요청 분석, 데이터 검토, 지시 발행, 최종 Audit(품질 확인)을 담당합니다. 이전 설명처럼 Llama 3.3 70B에 LoRA 학습 대신 프롬프트 주입을 사용하므로, 데이터는 주로 판단 기준(Rule-book), 워크플로우 로직, 검토 가이드라인(Rubric), Few-shot 예시 형태로 구성됩니다. 이는 IFRS 기준서에서 추출하거나, 기존 감사 가이드라인/템플릿에서 가져와 프롬프트로 변환합니다. 학습 데이터가 아닌 "지식 주입"이므로, 공식 문서 중심으로 수집.

목적: IFRS 감사관 마인드셋(예: "IFRS S2 15조항 준수 여부 확인"), State 전환 로직(1단계: 데이터 확인 → 2단계: 누락 재요청 등), Rubric(예: "기후 리스크 재무 영향 명시 여부 평가").
추천 출처 및 방법:
IFRS 기준서 (감사/준수 가이드라인):
URL: https://www.ifrs.org/issued-standards/ifrs-sustainability-standards-navigator/ (IFRS S1/S2 상세 요구사항, e.g., "S2 Paragraph 15: Financial impacts of climate risks").
형식: PDF (직접 다운로드: IFRS S1 PDF - https://www.ifrs.org/content/dam/ifrs/publications/pdf-standards-issb/english/2023/issued/part-a/issb-2023-a-ifrs-s1-general-requirements.pdf; IFRS S2 PDF - https://www.ifrs.org/content/dam/ifrs/publications/pdf-standards-issb/english/2023/issued/part-a/issb-2023-a-ifrs-s2-climate-related-disclosures.pdf).
수집 방법:
Browse_page (url="https://www.ifrs.org/issued-standards/ifrs-sustainability-standards-navigator/", instructions="Extract audit rubrics and compliance checklists for IFRS S2 climate risks and financial impacts section, summarize as JSON rules like {'rule': 'Must specify financial exposure in KRW', 'reference': 'S2 Para 15'}").
PDF 파싱: search_pdf_attachment (file_name="ifrs-s2.pdf", query="climate risks financial impacts requirements", mode="keyword") → 관련 조항 추출 후 Rubric으로 변환 (e.g., "이 문장은 재무 영향 구체적이지 않음 → 재작성 지시").
크롤링: code_execution (code: "import requests; from bs4 import BeautifulSoup; response = requests.get('https://www.ifrs.org/...'); soup = BeautifulSoup(response.text, 'html.parser'); print(soup.find_all('p', class_='requirement'))") → 텍스트 추출.


IFRS Implementation Guidance 및 Audit Toolkits:
URL: https://www.ifrs.org/supporting-implementation/ (IFRS 재단의 구현 지침서, e.g., "IFRS S2 Application Guidance.pdf").
형식: PDF.
수집 방법: Web_search (query="IFRS S2 audit checklist PDF", num_results=10) → 결과 URL에서 browse_page (instructions="Summarize compliance rubrics for governance and risk management sections"). 예: Deloitte나 KPMG의 무료 IFRS 감사 템플릿 PDF (e.g., "KPMG IFRS Sustainability Reporting Guide.pdf" 검색).

기존 감사 프레임워크 (Few-shot 데이터):
URL: https://www.globalreporting.org/ (GRI에서 IFRS 전환 가이드, but IFRS-focused).
형식: PDF/웹.
수집 방법: Web_search (query="IFRS S1 S2 compliance rubric examples PDF") → Few-shot 예시 생성 (e.g., "노드 선택 예: 데이터 부족 시 RAG 재호출"). SASB/TCFD에서 유사한 Rubric 크롤링.

추가 팁: Rubric 데이터셋은 약 100~200개 규칙으로 구성 (e.g., JSON: {"check": "Financial linkage present?", "fail_message": "재무 영향 강조 부족"}). 한국 공시 기준(K-IFRS) 추가: https://www.kasb.or.kr/ (KASB 사이트, PDF 크롤링).


Gen Node (에이전트) 데이터 출처
Gen Node는 RAG 팩트 시트를 바탕으로 IFRS 문체 문단 생성을 담당합니다. EXAONE 3.0 7.8B를 LoRA로 학습하므로, 훈련 데이터셋 (instruction-input-output 쌍)이 핵심입니다. 이전 예시처럼 TCFD/GRI/SASB 보고서를 IFRS S2 스타일(재무 연결 강조, 전문 용어, 객관적 톤)로 변환하는 데이터. 데이터는 기존 보고서에서 추출/합성.

목적: 재무적 연결(Financial Linkage, e.g., "투자 → 부채 감소"), IFRS 키워드(전환 위험, CapEx), 보고서 톤("~함" 격식체) 학습.
추천 출처 및 방법:
기존 ESG 보고서 (GRI/SASB/TCFD 기반 → IFRS 변환 데이터셋 구성):
URL: https://dart.fss.or.kr/ (한국 기업 지속가능성 보고서, e.g., "삼성전자 2024 Sustainability Report.pdf" – GRI/TCFD 기반).
형식: PDF.
수집 방법:
Browse_pdf_attachment (file_name="samsung-sustainability-2024.pdf", pages="1-100") → 섹션 추출 (e.g., "TCFD Risks" → input으로 사용, IFRS 스타일 output 수동/스크립트 생성).
Web_search (query="site:dart.fss.or.kr GRI SASB TCFD report PDF 2024", num_results=20) → 11개 보고서(이전 언급) 다운로드 후, code_execution (code: "import pandas as pd; df = pd.read_excel('reports.xlsx'); # Q&A 쌍 생성 로직") → 데이터셋 자동 생성 (e.g., 500개 쌍: {"instruction": "TCFD to IFRS convert", "input": "Scope 1: 12500 tCO2e", "output": "당사는 ... 재무적 회복탄력성 확보"}).

글로벌 예: https://www.sasb.org/company-use/sasb-reporters/ (SASB 리포터 목록, PDF 보고서).

IFRS 샘플 보고서 및 변환 가이드:
URL: https://www.ifrs.org/supporting-implementation/ifrs-sustainability-disclosure-standards/ (IFRS S1/S2 예시 문단, e.g., "Illustrative Examples.pdf").
형식: PDF (직접: https://www.ifrs.org/content/dam/ifrs/publications/pdf-standards-issb/english/2023/issued/part-b/issb-2023-b-ifrs-s2-illustrative-examples.pdf).
수집 방법: search_pdf_attachment (file_name="ifrs-s2-illustrative.pdf", query="climate strategy financial impacts examples", mode="keyword") → output 예시 추출 (e.g., 재무 연결 문장).
추가: Web_search (query="GRI to IFRS transition report examples PDF") → KPMG/EY 가이드 (e.g., "Deloitte GRI-SASB-IFRS Mapping Tool.pdf" – 매핑 테이블로 input-output 쌍 생성).

ESG 데이터 플랫폼 및 용어 사전:
URL: https://www.globalreporting.org/standards/gri-standards-glossaries/ (GRI Glossary PDF – 키워드 학습).
형식: PDF/Excel.
수집 방법: Browse_page (url="https://www.globalreporting.org/standards/gri-standards-glossaries/", instructions="Extract ESG terms like 'transition risks' with definitions, format as JSON for LoRA dataset"). TCFD Annex PDF로 변환 예시.

추가 팁: 데이터셋 규모: 500~1000개 쌍 (Contrastive Learning 유사). 한국 기업 보고서 10~20개 크롤링 (DART API 없으므로 Selenium: code_execution). LoRA 학습용: Hugging Face 데이터셋 (query="ESG report dataset") 검색.


전체 멀티모달 에이전트 구현 데이터 출처 (GRI/SASB/TCFD → IFRS 전환 및 문단 생성)

통합 출처: 위 Supervisor/Gen에 더해, RAG처럼 기업 내부 데이터(DART PDF)와 기준서(IFRS PDF)를 결합. 멀티모달(텍스트+이미지/표) 처리 시, PDF 내 표/차트 추출 (e.g., code_execution으로 pandas/openpyxl 사용).
전환 데이터셋 구성 팁: Web_search (query="GRI SASB TCFD to IFRS mapping guide PDF") → 매핑 문서 (e.g., "IFRS Foundation Transition Plan.pdf"). 11개 보고서 언급처럼, 실제 기업 PDF 50개 수집 → 스크립트로 input-output 쌍 생성.
법적/윤리 주의: 공공 PDF만 사용, 저작권 준수. 필요 시 API (e.g., Bloomberg ESG Data – but 무료 아님).

이 출처로 데이터 수집하면 에이전트 구현이 가능합니다. 구체적 PDF/URL 더 필요하시면 말씀하세요!













# 오케스트레이터 구현

문서에서 오케스트레이터 구현 방식을 확인 중입니다.


[3 tools called]


문서 기준 오케스트레이터 구현 방식 요약입니다.

## 문서의 오케스트레이터 구현 설계

### 1. 두 가지 구성 요소

#### A. SupervisorAgent (감사관)
- 역할: 워크플로우 제어 및 의사결정
- 모델: Llama 3.3 70B (Groq API)
- 주요 메서드:
  - `analyze_request()`: 사용자 요청 분석 및 DP 식별
  - `review_extraction()`: RAG 추출 결과 검토
  - `audit_generation()`: 생성 결과 최종 감사

#### B. IFRSAgentWorkflow (LangGraph 워크플로우)
- 역할: 노드 연결 및 실행 흐름 관리
- 프레임워크: LangGraph StateGraph
- 기능: 조건부 엣지, 반복 루프, 상태 관리

### 2. 구현 구조 (문서 기준)

```python
# 구조 개요
orchestrator/
├── supervisor.py      # SupervisorAgent 클래스
├── workflow.py        # IFRSAgentWorkflow 클래스
└── state.py          # IFRSAgentState (TypedDict)

agent/
├── rag_node.py
├── gen_node.py
└── validation_node.py
```

### 3. SupervisorAgent 구현 (문서 기준)

```python
class SupervisorAgent:
    """감사관 페르소나 - 워크플로우 제어"""
    
    def __init__(self, llm_client: GroqClient):
        self.llm = llm_client
        self.rulebook = self._load_rulebook()
    
    # 3가지 주요 메서드 (LangGraph 노드로 등록됨)
    
    def analyze(self, state: IFRSAgentState) -> IFRSAgentState:
        """1단계: 사용자 요청 분석 및 필요 DP 식별"""
        # LLM으로 쿼리 분석
        prompt = self._build_analysis_prompt(
            state["query"], 
            state["target_standards"]
        )
        response = self.llm.generate(prompt)
        decision = self._parse_decision(response)
        
        # 상태 업데이트
        state["target_dps"] = decision["required_dps"]
        state["current_node"] = "analyzing"
        state["audit_log"].append({
            "action": "analyze",
            "decision": decision,
            "timestamp": datetime.now()
        })
        
        return state
    
    def review(self, state: IFRSAgentState) -> IFRSAgentState:
        """2단계: RAG 추출 결과 검토"""
        required_dps = set(state["target_dps"])
        extracted_dps = set(fs["dp_id"] for fs in state["fact_sheets"])
        missing = required_dps - extracted_dps
        
        # 상태 업데이트
        state["current_node"] = "reviewing"
        state["audit_log"].append({
            "action": "review",
            "missing_dps": list(missing),
            "timestamp": datetime.now()
        })
        
        return state
    
    def audit(self, state: IFRSAgentState) -> IFRSAgentState:
        """3단계: 생성 결과 최종 감사"""
        validation_result = state["validation_results"][-1]
        
        # 그린워싱 체크
        if validation_result["greenwashing_risk"] > 0.7:
            state["audit_log"].append({
                "action": "reject",
                "reason": "greenwashing_risk",
                "timestamp": datetime.now()
            })
            return state
        
        # IFRS 준수 체크
        if validation_result["compliance_score"] < 0.8:
            missing_dps = self._identify_missing_dps(
                state["generated_sections"],
                state["target_dps"]
            )
            state["audit_log"].append({
                "action": "request_revision",
                "missing_dps": missing_dps,
                "timestamp": datetime.now()
            })
        
        state["current_node"] = "auditing"
        return state
```

### 4. IFRSAgentWorkflow 구현 (문서 기준)

```python
class IFRSAgentWorkflow:
    """LangGraph 워크플로우"""
    
    def __init__(self, config: Dict = None):
        # 노드 인스턴스 생성
        self.supervisor = SupervisorAgent(config.get("supervisor", {}))
        self.rag_node = RAGNode(config.get("rag", {}))
        self.gen_node = GenNode(config.get("gen", {}))
        self.validation_node = ValidationNode(config.get("validation", {}))
        
        # 그래프 빌드
        self.graph = self._build_graph()
    
    def _build_graph(self) -> StateGraph:
        """워크플로우 그래프 구성"""
        workflow = StateGraph(IFRSAgentState)
        
        # 노드 추가 (Supervisor 메서드를 노드로 등록)
        workflow.add_node("supervisor_analyze", self.supervisor.analyze)
        workflow.add_node("rag_node", self.rag_node.process)
        workflow.add_node("supervisor_review", self.supervisor.review)
        workflow.add_node("gen_node", self.gen_node.process)
        workflow.add_node("validation_node", self.validation_node.process)
        workflow.add_node("supervisor_audit", self.supervisor.audit)
        
        # 엣지 추가
        workflow.set_entry_point("supervisor_analyze")
        workflow.add_edge("supervisor_analyze", "rag_node")
        workflow.add_edge("rag_node", "supervisor_review")
        
        # 조건부 엣지: 데이터 충분성 검토
        workflow.add_conditional_edges(
            "supervisor_review",
            self._check_data_sufficiency,  # 조건 함수
            {
                "rag_node": "rag_node",    # 재검색
                "gen_node": "gen_node"      # 진행
            }
        )
        
        workflow.add_edge("gen_node", "validation_node")
        workflow.add_edge("validation_node", "supervisor_audit")
        
        # 조건부 엣지: 최종 감사
        workflow.add_conditional_edges(
            "supervisor_audit",
            self._check_quality,
            {
                "gen_node": "gen_node",    # 재생성
                "end": END                  # 완료
            }
        )
        
        return workflow.compile()
    
    def _check_data_sufficiency(self, state: IFRSAgentState) -> str:
        """데이터 충분성 검토 후 다음 노드 결정"""
        required = set(state["target_dps"])
        extracted = set(fs["dp_id"] for fs in state["fact_sheets"])
        missing = required - extracted
        
        if missing and state["iteration_count"] < 3:
            state["iteration_count"] += 1
            return "rag_node"  # 재검색
        return "gen_node"  # 진행
    
    def _check_quality(self, state: IFRSAgentState) -> str:
        """품질 검토 후 다음 노드 결정"""
        if not state["validation_results"]:
            return "end"
        
        latest = state["validation_results"][-1]
        
        if latest.get("greenwashing_risk", 0) > 0.7:
            return "end"  # 거부
        
        if latest.get("compliance_score", 1) < 0.8:
            if state["iteration_count"] < 3:
                state["iteration_count"] += 1
                return "gen_node"  # 재생성
        
        return "end"  # 완료
    
    async def run(self, query: str, ...) -> Dict:
        """워크플로우 실행"""
        initial_state: IFRSAgentState = {
            "query": query,
            "documents": documents,
            "target_standards": target_standards,
            "fiscal_year": fiscal_year,
            "company_id": company_id,
            "current_node": "entry",
            "iteration_count": 0,
            "target_dps": [],
            "fact_sheets": [],
            "generated_sections": [],
            "validation_results": [],
            "audit_log": [],
            # ... 기타 필드
        }
        
        # 실행
        result = await self.graph.ainvoke(initial_state)
        return result
```

### 5. 워크플로우 그래프 구조

```
Entry
  ↓
supervisor_analyze  (Supervisor.analyze)
  ↓
rag_node  (RAGNode.process)
  ↓
supervisor_review  (Supervisor.review)
  ↓
[조건부] check_data_sufficiency
  ├─→ rag_node (재검색)
  └─→ gen_node (진행)
        ↓
validation_node  (ValidationNode.process)
  ↓
supervisor_audit  (Supervisor.audit)
  ↓
[조건부] check_quality
  ├─→ gen_node (재생성)
  └─→ END (완료)
```

### 6. 핵심 설계 포인트

#### A. Supervisor는 3개의 노드로 분리
- `supervisor_analyze`: 분석 단계
- `supervisor_review`: 검토 단계
- `supervisor_audit`: 감사 단계

#### B. 조건부 엣지로 반복 루프 구현
```python
# 데이터 부족 시 재검색
if missing_dps and iteration_count < 3:
    return "rag_node"  # 루프

# 품질 미달 시 재생성
if compliance_score < 0.8 and iteration_count < 3:
    return "gen_node"  # 루프
```

#### C. 상태 공유
- 모든 노드는 `IFRSAgentState`를 받고 반환
- LangGraph가 상태를 자동으로 관리

### 7. 문서의 파일 구조 제안

문서에서는 다음 구조를 제안합니다:

```
ifrs_agent/
├── base/
│   └── state.py          # IFRSAgentState
├── agent/
│   ├── supervisor.py     # SupervisorAgent
│   ├── workflow.py       # IFRSAgentWorkflow
│   ├── rag_node.py
│   ├── gen_node.py
│   └── validation_node.py
```

하지만 제안하신 구조가 더 명확합니다:

```
ifrs_agent/
├── orchestrator/
│   ├── supervisor.py     # SupervisorAgent
│   ├── workflow.py       # IFRSAgentWorkflow
│   └── state.py          # IFRSAgentState
└── agent/
    ├── base.py
    ├── rag_node.py
    ├── gen_node.py
    └── validation_node.py
```

## 요약

문서 기준 오케스트레이터는:
1. SupervisorAgent: 3개 메서드(analyze, review, audit)로 워크플로우 제어
2. IFRSAgentWorkflow: LangGraph StateGraph로 노드 연결 및 실행
3. 조건부 엣지: 반복 루프 구현
4. 상태 공유: IFRSAgentState로 노드 간 데이터 전달

제안하신 `orchestrator/`와 `agent/` 분리 구조가 문서 설계와 잘 맞습니다.





생성한 파일들을 확인하고 각 파일의 역할과 구조를 설명합니다.


[6 tools called]


생성한 파일들의 역할과 구조를 설명합니다.

## 1. `orchestrator/state.py` - 워크플로우 상태 정의

### 역할
LangGraph 워크플로우에서 공유되는 상태를 TypedDict로 정의합니다.

### 주요 구조
```12:41:ai/ifrs_agent/orchestrator/state.py
    # 입력 정보
    query: str                              # 사용자 쿼리
    documents: List[str]                    # 업로드된 문서 경로
    target_standards: List[str]            # 대상 기준서 (IFRS_S1, IFRS_S2 등)
    fiscal_year: int                        # 회계연도
    company_id: str                         # 기업 식별자
    
    # 처리 상태
    current_node: str                       # 현재 실행 중인 노드
    iteration_count: int                    # 반복 횟수 (재시도 추적)
    status: str                             # 워크플로우 상태 (initialized, analyzing, retrieving, etc.)
    
    # 추출 데이터
    target_dps: List[str]                   # 필요한 DP 목록 (Supervisor가 식별)
    fact_sheets: List[Dict[str, Any]]      # 추출된 팩트 시트 (RAG Node 출력)
    yearly_data: Dict[int, Dict[str, Any]] # 연도별 데이터 (2022, 2023, 2024)
    
    # 생성 결과
    generated_sections: List[Dict[str, Any]]  # 생성된 섹션들 (Gen Node 출력)
    
    # 검증 결과
    validation_results: List[Dict[str, Any]]  # 검증 결과 목록 (Validation Node 출력)
    
    # 기업 아이덴티티
    corporate_identity: Dict[str, Any]     # 컬러, 스타일 등 (Design Node용)
    
    # 메타 정보
    reference_sources: List[str]           # 참조 출처 목록
    audit_log: List[Dict[str, Any]]       # 감사 로그 (모든 결정 기록)
    errors: List[str]                      # 에러 목록
```

### 특징
- TypedDict로 타입 안정성 제공
- 노드 간 데이터 전달을 위한 공유 상태
- 입력 → 처리 → 출력 흐름을 추적

---

## 2. `orchestrator/supervisor.py` - Supervisor Agent (감사관)

### 역할
중앙 오케스트레이터. 워크플로우 제어와 품질 검수를 담당합니다.

### 주요 구성 요소

#### IFRS_RULEBOOK (22-65줄)
- 기준서별 필수 DP와 검증 규칙 정의
- IFRS_S1, IFRS_S2 기준서 지원

#### SupervisorAgent 클래스

1. `__init__()` (106-125줄)
   - Groq API 클라이언트 초기화
   - 설정 로드 및 Rulebook 로드

2. `analyze()` (127-168줄)
   - 사용자 요청 분석
   - LLM으로 필요한 DP 식별
   - `target_dps` 업데이트 및 감사 로그 기록

3. `review()` (170-203줄)
   - RAG 추출 결과 검토
   - 필수 DP 누락 여부 확인
   - 감사 로그 기록

4. `audit()` (205-283줄)
   - 최종 감사
   - 그린워싱 위험도 체크 (0.7 초과 시 거부)
   - IFRS 준수 점수 체크 (0.8 미만 시 재생성 요청)
   - 누락 DP 및 재무 연결성 부족 섹션 식별

5. 헬퍼 메서드
   - `_build_analysis_prompt()`: 분석 프롬프트 구성
   - `_parse_decision()`: LLM JSON 응답 파싱
   - `_identify_missing_dps()`: 누락 DP 식별
   - `_check_financial_linkage()`: 재무 연결성 검증

### 특징
- 감사관 페르소나로 동작
- 3단계 프로세스: 분석 → 검토 → 감사
- 에러 처리 및 로깅 포함

---

## 3. `orchestrator/workflow.py` - LangGraph 워크플로우

### 역할
LangGraph로 노드를 연결하고 실행 흐름을 관리합니다.

### 주요 구성 요소

#### IFRSAgentWorkflow 클래스

1. `__init__()` (27-49줄)
   - Supervisor 인스턴스 생성
   - 노드 인스턴스 초기화 (현재는 None)
   - 그래프 빌드

2. `_build_graph()` (51-114줄)
   - LangGraph StateGraph 구성
   - 노드 추가:
     - `supervisor_analyze`: 요청 분석
     - `rag_node`: 데이터 추출 (더미)
     - `supervisor_review`: 추출 결과 검토
     - `gen_node`: 문단 생성 (더미)
     - `validation_node`: 검증 (더미)
     - `supervisor_audit`: 최종 감사
   - 엣지 연결:
     - 순차 엣지: analyze → rag → review → gen → validation → audit
     - 조건부 엣지:
       - `supervisor_review` → 데이터 충분성에 따라 `rag_node` 또는 `gen_node`
       - `supervisor_audit` → 품질에 따라 `gen_node` 또는 `END`

3. 조건부 분기 함수
   - `_check_data_sufficiency()` (116-136줄): 데이터 충분성 검토
   - `_check_quality()` (138-168줄): 품질 검토

4. 더미 노드 함수 (170-204줄)
   - `_dummy_rag_node()`, `_dummy_gen_node()`, `_dummy_validation_node()`
   - 실제 노드 구현 전까지 사용

5. `set_nodes()` (206-222줄)
   - 실제 노드 인스턴스 설정
   - 그래프 재빌드

6. 실행 메서드
   - `run()` (224-281줄): 비동기 실행
   - `run_sync()` (283-302줄): 동기 실행 (테스트용)

### 워크플로우 흐름
```
[Entry] 
  ↓
[supervisor_analyze] → DP 식별
  ↓
[rag_node] → 데이터 추출
  ↓
[supervisor_review] → 데이터 충분성 검토
  ├─ 데이터 부족 → [rag_node] (재검색)
  └─ 데이터 충분 → [gen_node]
  ↓
[gen_node] → 문단 생성
  ↓
[validation_node] → 검증
  ↓
[supervisor_audit] → 최종 감사
  ├─ 품질 미달 → [gen_node] (재생성)
  └─ 품질 통과 → [END]
```

---

## 4. `orchestrator/__init__.py` - 모듈 초기화

### 역할
orchestrator 패키지의 공개 API 정의

### 내용
```5:13:ai/ifrs_agent/orchestrator/__init__.py
from ifrs_agent.orchestrator.state import IFRSAgentState
from ifrs_agent.orchestrator.supervisor import SupervisorAgent
from ifrs_agent.orchestrator.workflow import IFRSAgentWorkflow

__all__ = [
    "IFRSAgentState",
    "SupervisorAgent",
    "IFRSAgentWorkflow",
]
```

### 사용 예시
```python
from ifrs_agent.orchestrator import IFRSAgentWorkflow, IFRSAgentState
```

---

## 5. `config/settings.py` - 애플리케이션 설정

### 역할
환경 변수와 설정을 중앙 관리합니다.

### 주요 구성 요소

#### Settings 클래스 (8-37줄)
- Pydantic BaseSettings 상속
- 필드:
  - API Keys: Groq, OpenAI, DART
  - Database: DATABASE_URL, NEON_DATABASE_URL
  - Model Settings: 각 노드별 모델명
  - Processing: max_retries, chunk_size 등
  - Groq Settings: temperature, max_tokens

#### get_settings() 함수 (40-43줄)
- 싱글톤 패턴 (lru_cache)
- 전역에서 동일한 설정 인스턴스 사용

### 특징
- `.env` 파일 자동 로드
- 타입 안정성 (Pydantic)
- 환경 변수 기본값 제공

---

## 6. `orchestrator/example_usage.py` - 사용 예제

### 역할
오케스트레이터 기본 사용법 예제

### 주요 내용
```9:36:ai/ifrs_agent/orchestrator/example_usage.py
async def main():
    """기본 사용 예제"""
    
    # 워크플로우 초기화
    workflow = IFRSAgentWorkflow(
        config={
            "max_retries": 3,
            "enable_checkpointing": False
        }
    )
    
    # 워크플로우 실행
    result = await workflow.run(
        query="IFRS S2 기준으로 기후 관련 위험과 기회를 보고서에 포함해주세요",
        target_standards=["IFRS_S2"],
        fiscal_year=2024,
        company_id="test_company_001"
    )
    
    # 결과 출력
    print(f"상태: {result['status']}")
    print(f"식별된 DP: {len(result['target_dps'])}개")
    print(f"추출된 팩트 시트: {len(result['fact_sheets'])}개")
    print(f"생성된 섹션: {len(result['generated_sections'])}개")
    print(f"감사 로그: {len(result['audit_log'])}개 항목")
    
    if result.get("errors"):
        print(f"에러: {result['errors']}")
```

---

## 7. `config/__init__.py` - Config 모듈 초기화

### 역할
config 패키지의 공개 API 정의

### 내용
```1:4:ai/ifrs_agent/config/__init__.py
"""Config 모듈"""
from ifrs_agent.config.settings import Settings, get_settings

__all__ = ["Settings", "get_settings"]
```

---

## 전체 아키텍처 요약

```
orchestrator/
├── state.py          → 상태 정의 (TypedDict)
├── supervisor.py     → 감사관 Agent (3단계 프로세스)
├── workflow.py       → LangGraph 워크플로우 (노드 연결)
├── __init__.py      → 모듈 초기화
└── example_usage.py  → 사용 예제

config/
├── settings.py       → 설정 관리 (Pydantic)
└── __init__.py      → 모듈 초기화
```

## 핵심 설계 원칙

1. 관심사 분리: 상태, 감사관, 워크플로우 분리
2. 확장성: `set_nodes()`로 노드 교체 가능
3. 타입 안정성: TypedDict, Pydantic 사용
4. 에러 처리: try-except와 로깅 포함
5. 테스트 용이성: 더미 노드와 동기 실행 메서드 제공

이 구조로 오케스트레이터가 동작하며, 이후 노드 구현 시 `set_nodes()`로 연결하면 됩니다.






# 노드 구현

문서에서 노드 구현 설명을 확인 중입니다.


[3 tools called]


문서 기준으로 각 노드 구현 설명입니다.

## 노드 구현 개요

문서에 따르면 5개의 노드가 있습니다:

### 1. Supervisor (오케스트레이터) - 이미 구현됨

**모델**: Llama 3.3 70B Versatile (Groq API)

**역할**:
- 감사관(Auditor) 페르소나로 동작
- 사용자 요청 분석 및 DP 식별
- 각 노드에 작업 지시
- 데이터 충분성 검토
- 최종 품질 감사 (Audit)
- 그린워싱 방지 감시

**구현 특징**:
- 3단계 프로세스: `analyze()` → `review()` → `audit()`
- IFRS Rulebook 주입 (기준서별 필수 DP 및 검증 규칙)
- LLM 기반 의사결정 (JSON 형식 응답 파싱)
- 누락 DP 식별 및 재무 연결성 검증

---

### 2. RAG Node (검색 및 추출)

**모델**: Llama 3.1 70B Tool-Use (Groq API)

**역할**:
- 데이터 추출가 페르소나
- 쿼리 최적화 및 재구성 (Tool Calling 활용)
- 하이브리드 검색 (Dense + Sparse)
- PDF 목차 기반 정밀 파싱
- 멀티모달 처리: 표·이미지 추출 및 캡셔닝
- 3개년 시계열 데이터 추출
- JSON 팩트 시트 생성

**주요 구현 컴포넌트**:

1. Tool Calling 도구:
   - `hybrid_search`: 벡터 DB + BM25 하이브리드 검색
   - `extract_data_point`: 검색 결과에서 DP 값 추출

2. 하이브리드 검색 엔진:
   - Dense 검색 (임베딩 기반)
   - Sparse 검색 (BM25)
   - Reciprocal Rank Fusion (RRF) 점수 융합

3. 멀티모달 처리:
   - `TableExtractor`: LlamaParse 기반 표 추출
   - `ImageProcessor`: 이미지 캡셔닝 (GPT-4o-mini Vision 또는 Donut)

4. 크롤링 모듈:
   - DART 공시 데이터 크롤링
   - 미디어 기사 크롤링
   - 경쟁사 보고서 크롤링

5. 팩트 시트 생성:
   - DP별 구조화된 데이터 추출
   - 연도별 값, 단위, 출처, 신뢰도 점수 포함

**출력 형식**:
```python
FactSheet(
    dp_id: str,
    dp_name: str,
    values: Dict[int, Any],  # {2022: value, 2023: value, 2024: value}
    unit: str,
    source: str,
    page_reference: str,
    confidence: float
)
```

---

### 3. Gen Node (문단 생성)

**모델**: EXAONE 3.0 7.8B Instruct (LoRA 학습)

**역할**:
- 전문 작가 페르소나
- IFRS 전문 문체 적용
- 재무적 연결성(Financial Linkage) 강조
- 시계열 분석 및 추세 설명
- 근거 주석 자동 생성
- 이전 년도 데이터 조합 추천

**주요 구현 컴포넌트**:

1. LoRA 학습 설정:
   - Base Model: `LGAI-EXAONE/EXAONE-3.0-7.8B-Instruct`
   - LoRA r=16, alpha=32, dropout=0.05
   - 4bit 양자화 (RTX 4070 Super 12GB 대응)
   - 학습 데이터: GRI→IFRS 변환 데이터셋

2. 문단 생성 로직:
   - 섹션별 팩트 시트 필터링
   - IFRS 문체 프롬프트 구성
   - 재무 연결성 자동 추출
   - 근거 주석 추가

3. 생성 프롬프트 템플릿:
   - 재무적 연결성 명시 규칙
   - 정량적 근거 포함 규칙
   - 시계열 분석 규칙
   - IFRS 문체 유지 규칙

4. 이전 년도 데이터 추천:
   - 추세 계산 (증가/감소/유지)
   - 전년 대비 변화율 계산
   - 서술 문장 추천

**출력 형식**:
```python
GeneratedParagraph(
    section_id: str,
    content: str,  # 생성된 문단
    sources: List[str],
    financial_linkage: str,  # 재무 연결 설명
    suggested_visuals: List[str]
)
```

---

### 4. Validation Node (검증)

**모델**: Llama 3.1 8B Instant (Groq API)

**역할**:
- 입력 데이터 범위 검증
- 공시 데이터와 비교 검증
- 그린워싱 표현 탐지
- IFRS 준수 여부 확인

**주요 구현 컴포넌트**:

1. 범위 검증 규칙:
   - 백분율: 0-100 범위
   - 임직원 수: 1-10,000,000
   - 배출 집약도: 0-1000 tCO2e/억원
   - 성별 비율 합계: 100% 체크

2. 그린워싱 탐지:
   - 패턴 기반 탐지 (정규식)
     - 과장된 표현: "세계 최고", "100% 친환경"
     - 모호한 약속: "노력할 예정" (구체적 계획 없음)
     - 근거 없는 친환경 주장
   - LLM 기반 심층 분석
   - 위험 점수 계산 (0-1)

3. 공시 데이터 비교:
   - DART 공시 데이터와 비교
   - 10% 이상 차이 시 경고
   - 30% 이상 차이 시 High Severity

**출력 형식**:
```python
ValidationResult(
    is_valid: bool,
    errors: List[ValidationError],
    warnings: List[ValidationWarning],
    greenwashing_risk: float,  # 0-1
    compliance_score: float,   # 0-1
    compliance_issues: List[str]
)
```

---

### 5. Design Recommendation Node (디자인 추천)

**모델**: Llama 3.3 70B (Groq API)

**역할**:
- 브랜드 디자이너 페르소나
- 기업 BI 컬러/스타일 분석
- IFRS 구조에 맞는 차트 타입 추천
- 경쟁사 벤치마킹 기반 디자인 가이드

**주요 구현 컴포넌트**:

1. 기업 BI 추출:
   - 주요 컬러 추출
   - 스타일 분석 (professional, modern 등)

2. 경쟁사 분석:
   - 동일 업종 경쟁사 보고서 수집
   - 디자인 요소 추출 (컬러, 차트 타입, 스타일)

3. 디자인 가이드 생성:
   - 섹션별 차트 타입 추천 (waterfall, bar, line, scatter, heatmap)
   - 브랜드 컬러와 조화되는 컬러 스킴
   - IFRS 구조 적합성 설명
   - 경쟁사 대비 차별화 포인트

**출력 형식**:
```python
DesignRecommendation(
    section_id: str,
    chart_type: str,
    color_scheme: {
        "primary": "#HEX",
        "secondary": "#HEX",
        "accent": "#HEX"
    },
    rationale: str,
    brand_alignment: str,
    competitor_insight: str
)
```

---

## 노드 간 통신

### 메시지 형식
```python
NodeMessage(
    from_node: str,
    to_node: str,
    message_type: "instruction" | "result" | "error" | "feedback",
    payload: Dict[str, Any],
    timestamp: datetime,
    correlation_id: str  # 요청 추적용
)
```

### 에러 처리
- `recoverable`: 복구 가능 여부
- `suggested_action`: 제안된 조치
- Supervisor로 에러 전파

---

## 구현 순서 제안

1. RAG Node (검색/추출)
2. Validation Node (검증)
3. Gen Node (문단 생성)
4. Design Node (디자인 추천, 선택)

각 노드는 `IFRSAgentState`를 입력받아 수정 후 반환하는 `process()` 메서드를 구현하면 됩니다.

어떤 노드부터 구현할까요?



각 노드 구현 파일 확인 중입니다.


[7 tools called]


각 노드 구현 파일 설명입니다.

## 노드 구현 파일 구조

### 1. **`base.py`** - 기본 노드 클래스

모든 노드의 기본 클래스입니다.

**주요 기능:**
- `BaseNode` 추상 클래스 정의
- 모든 노드가 상속받는 공통 인터페이스 제공
- `process()` 추상 메서드: 각 노드가 구현해야 하는 메인 로직
- `_update_state()` 헬퍼 메서드: 상태 업데이트 공통 로직

**설계 목적:**
- 노드 간 일관된 인터페이스 보장
- 공통 기능 중복 제거
- 타입 안정성 및 확장성

---

### 2. **`rag_node.py`** - RAG Node (검색 및 추출)

데이터 추출을 담당하는 노드입니다.

**주요 기능:**
- 쿼리 최적화: 사용자 쿼리를 검색 쿼리로 변환
- 하이브리드 검색: Dense + Sparse 검색 (현재 더미)
- 외부 크롤링: DART 공시 데이터 등 (현재 더미)
- 멀티모달 추출: PDF 표/이미지 추출 (현재 더미)
- DP 추출: LLM을 사용한 구조화된 팩트 시트 생성
- 연도별 데이터 정리: `_organize_by_year()`

**구현 상태:**
- ✅ LLM 기반 DP 추출 구현됨
- ⚠️ 하이브리드 검색, 크롤링, 멀티모달은 TODO

**사용 모델:**
- `llama-3.1-70b-versatile` (Groq) - Tool-Use 버전 권장

---

### 3. **`gen_node.py`** - Gen Node (문단 생성)

IFRS 문체로 보고서 문단을 생성하는 노드입니다.

**주요 기능:**
- 팩트 시트 기반 문단 생성
- IFRS 문체 적용
- 재무 연결성 강조
- 시계열 분석 및 추세 설명

**구현 상태:**
- ⚠️ 현재 더미 구현 (실제 LoRA 모델 미연결)
- TODO: EXAONE 3.0 7.8B LoRA 모델 로드 필요

**예상 구조:**
```python
# TODO: 실제 구현 시
- self.model = self._load_lora_model(config.get("model_path"))
- self.tokenizer = self._load_tokenizer(config.get("model_path"))
- IFRS 문체 프롬프트 구성
- 재무 연결성 추출
- 근거 주석 추가
```

---

### 4. **`validation_node.py`** - Validation Node (검증)

입력 데이터 및 생성 결과를 검증하는 노드입니다.

**주요 기능:**
- 입력 데이터 검증: 범위 검사, 합계 검사
- 그린워싱 탐지: 패턴 기반 + LLM 기반 분석
- IFRS 준수 검사: 재무 연결성, 출처 확인
- 위험 점수 계산: `_calculate_risk_score()`

**구현된 검증 규칙:**
- 백분율 범위 검증 (0-100)
- 임직원 수 범위 검증
- 배출 집약도 검증
- 성별 비율 합계 검증

**그린워싱 패턴:**
- 과장된 표현: "세계 최고", "100% 친환경"
- 모호한 약속: "노력할 예정" (구체적 계획 없음)
- 근거 없는 친환경 주장

**사용 모델:**
- `llama-3.1-8b-instant` (Groq) - 빠른 검증, 저비용

---

### 5. **`design_node.py`** - Design Node (디자인 추천)

기업 BI/CI를 반영한 시각화 가이드를 제공하는 노드입니다.

**주요 기능:**
- 기업 BI 추출 (예정)
- 경쟁사 분석 (예정)
- 섹션별 디자인 추천 생성
- 차트 타입 추천
- 컬러 스킴 제안

**구현 상태:**
- ⚠️ 현재 더미 구현
- TODO: Llama 3.3 70B (Groq) 사용 예정

**예상 출력:**
```python
{
    "section_id": "S2-15-a",
    "chart_type": "bar_chart",
    "color_scheme": {
        "primary": "#0066CC",
        "secondary": "#003366",
        "accent": "#FFD700"
    },
    "rationale": "시계열 데이터에 적합한 막대 그래프",
    "brand_alignment": "기업 CI 컬러 반영"
}
```

---

### 6. **`__init__.py`** - 모듈 초기화

모든 노드를 export하는 모듈 초기화 파일입니다.

**역할:**
- 노드 클래스들을 외부에서 import 가능하도록 설정
- `__all__` 리스트로 공개 API 정의

**사용 예시:**
```python
from ifrs_agent.agent import RAGNode, GenNode, ValidationNode, DesignNode
```

---

## 파일 구조 요약

```
ai/ifrs_agent/agent/
├── __init__.py          # 모듈 초기화 및 export
├── base.py              # BaseNode 추상 클래스
├── rag_node.py          # RAG Node (검색 및 추출) ✅ 부분 구현
├── gen_node.py          # Gen Node (문단 생성) ⚠️ 더미
├── validation_node.py   # Validation Node (검증) ✅ 구현됨
└── design_node.py       # Design Node (디자인 추천) ⚠️ 더미
```

## 구현 상태 요약

| 노드 | 구현 상태 | 주요 기능 | 모델 |
|------|----------|----------|------|
| **BaseNode** | ✅ 완료 | 공통 인터페이스 | - |
| **RAGNode** | ✅ 부분 구현 | DP 추출 구현, 검색/크롤링 TODO | Llama 3.1 70B |
| **GenNode** | ⚠️ 더미 | LoRA 모델 연결 필요 | EXAONE 3.0 7.8B (예정) |
| **ValidationNode** | ✅ 완료 | 검증, 그린워싱 탐지 | Llama 3.1 8B |
| **DesignNode** | ⚠️ 더미 | LLM 기반 추천 필요 | Llama 3.3 70B (예정) |

모든 노드는 `BaseNode`를 상속받아 일관된 인터페이스를 제공하며, LangGraph 워크플로우에서 사용됩니다.


# gen node

{"instruction": "다음 팩트 시트 데이터를 기반으로 IFRS S2 기후 리스크의 재무적 영향 섹션을 작성하세요. 재무적 연결성, 정량적 근거, 시계열 분석을 포함하여 객관적이고 전문적인 IFRS 문체로 작성하세요.", "input": "## 팩트 시트\n\n### DP: S2-15-a (물리적 리스크)\n- 2022년: 직접 재무적 영향 없음\n- 2023년: 폭염으로 인한 냉방비용 증가 2.3억원\n- 2024년: 태풍 피해 복구비용 5.1억원\n- 단위: 원\n- 출처: 재무제표 주석, 환경부 기후 데이터\n- 재무 연결: 재무상태표 '환경비용' 항목\n\n### DP: S2-15-b (전환 리스크)\n- 2022년: 탄소세 부담 0원\n- 2023년: 탄소배출권 구매비용 12억원\n- 2024년: 탄소배출권 구매비용 18억원 (전년 대비 50% 증가)\n- 단위: 원\n- 출처: DART 공시, 내부 회계 기록\n- 재무 연결: 손익계산서 '환경비용', 재무상태표 '탄소배출권' 자산\n\n## 작성 대상\n- 기준서: IFRS_S2\n- 섹션: 기후 리스크의 재무적 영향\n- 필수 DP: S2-15-a, S2-15-b", "output": "본 보고기간 동안 기후 관련 리스크가 재무제표에 미친 영향을 다음과 같이 보고합니다.\n\n**물리적 리스크의 재무적 영향**\n2023년 폭염으로 인한 냉방비용 증가분은 2.3억원을 기록하였으며, 이는 재무상태표의 '환경비용' 항목에 반영되었습니다. 2024년에는 태풍 피해 복구비용으로 5.1억원이 추가 발생하였으며, 전년 대비 121.7% 증가한 수준입니다. 이러한 물리적 리스크는 기후변화로 인한 극단적 기상 현상의 빈도 증가와 연관되어 있으며, 향후 재무적 영향이 확대될 가능성이 있습니다.\n\n**전환 리스크의 재무적 영향**\n탄소배출권 구매비용은 2023년 12억원에서 2024년 18억원으로 전년 대비 50.0% 증가하였습니다. 이는 손익계산서의 '환경비용' 항목에 반영되며, 동시에 재무상태표에 '탄소배출권' 자산으로 인식됩니다. 탄소 규제 강화에 따른 배출권 가격 상승이 주요 원인으로 분석됩니다.\n\n---\n**재무적 영향**: 물리적 리스크는 환경비용으로, 전환 리스크는 환경비용 및 탄소배출권 자산으로 재무제표에 반영됨\n**출처**: 재무제표 주석, DART 공시, 환경부 기후 데이터, 내부 회계 기록\n**추천 시각화**: 시계열 막대 그래프(연도별 비용 추이), 폭포형 차트(리스크 요인별 영향 분석)"}





## DP 추출 CLI 명령어

### 1. IFRS S2 (Climate-related Disclosures)

```bash
# 기본 실행 (임베딩 생성 포함)
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/issb-2023-a-ifrs-s2-climate-related-disclosures.pdf --standard IFRS_S2

# LLM 보조 한국어 이름 추출 사용
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/issb-2023-a-ifrs-s2-climate-related-disclosures.pdf --standard IFRS_S2 --use-llm

# 임베딩 생성 없이 실행
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/issb-2023-a-ifrs-s2-climate-related-disclosures.pdf --standard IFRS_S2 --no-embeddings
```

### 2. IFRS S1 (General Requirements)

```bash
# 기본 실행
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/issb-2023-a-ifrs-s1-general-requirements-for-disclosure-of-sustainability-related-financial-information.pdf --standard IFRS_S1

# LLM 보조 사용
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/issb-2023-a-ifrs-s1-general-requirements-for-disclosure-of-sustainability-related-financial-information.pdf --standard IFRS_S1 --use-llm
```

### 3. TCFD Report (2017)

```bash
# 기본 실행 (LlamaParse 또는 Unstructured 자동 선택)
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/FINAL-2017-TCFD-Report.pdf --standard TCFD

# PyMuPDF 강제 사용
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/FINAL-2017-TCFD-Report.pdf --standard TCFD --parser pymupdf

# LLM 보조 사용
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/FINAL-2017-TCFD-Report.pdf --standard TCFD --use-llm
```

### 4. TCFD Implementing Guidance (2021)

```bash
# 기본 실행
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/2021-TCFD-Implementing_Guidance.pdf --standard TCFD

# LLM 보조 사용
python -m ifrs_agent.scripts.extract_dps_from_standards ifrs_agent/data/2021-TCFD-Implementing_Guidance.pdf --standard TCFD --use-llm
```

## 실행 위치

프로젝트 루트(`ifrsseed`)에서 실행:

```bash
# Windows PowerShell
cd C:\Users\hi\Documents\ifrsseed

# 또는 Linux/Mac
cd ~/Documents/ifrsseed
```



# GRI 1: Foundation 2021 (LLM 보조)
python -m ifrs_agent.scripts.extract_dps_from_standards "ifrs_agent/data/GRI 1_ Foundation 2021.pdf" --standard GRI --use-llm

# GRI 2: General Disclosures 2021 (LLM 보조)
python -m ifrs_agent.scripts.extract_dps_from_standards "ifrs_agent/data/GRI 2_ General Disclosures 2021.pdf" --standard GRI --use-llm

# GRI 3: Material Topics 2021 (LLM 보조)
python -m ifrs_agent.scripts.extract_dps_from_standards "ifrs_agent/data/GRI 3_ Material Topics 2021.pdf" --standard GRI --use-llm

# GRI 102: Climate Change 2025 (LLM 보조)
python -m ifrs_agent.scripts.extract_dps_from_standards "ifrs_agent/data/GRI 102_ Climate Change 2025.pdf" --standard GRI --use-llm

# GRI 103: Energy 2025 (LLM 보조)
python -m ifrs_agent.scripts.extract_dps_from_standards "ifrs_agent/data/GRI 103_ Energy 2025.pdf" --standard GRI --use-llm

## 옵션 설명

| 옵션                | 설명                                           | 기본값     |
| ----------------- | -------------------------------------------- | ------- |
| `--standard`      | 기준서 코드 (IFRS_S1, IFRS_S2, GRI, TCFD, SASB)   | IFRS_S2 |
| `--use-llm`       | LLM 보조 한국어 이름 추출 (Groq API 필요)               | False   |
| `--no-embeddings` | 임베딩 생성 안 함 (빠른 테스트용)                         | False   |
| `--parser`        | 강제 파서 선택 (pymupdf, llamaparse, unstructured) | 자동 선택   |

## 실행 순서 권장

1. IFRS S2 (가장 중요)
2. IFRS S1
3. TCFD (2개 파일)

## 주의사항

- `--use-llm` 사용 시 `GROQ_API_KEY` 환경변수 필요
- DB에 이미 존재하는 DP는 중복 저장되지 않음 (dp_id 또는 dp_code 기준)
- 임베딩 생성은 시간이 걸릴 수 있음 (BGE-M3 모델 로딩 포함)

모든 명령어는 프로젝트 루트에서 실행하세요.



네, **Full set of GRI Standards V1.1 ZIP**은 꽤 방대합니다. (약 40~50개 정도의 PDF 파일 포함, 전체 크기 추정 50~100MB 정도일 가능성 높음 – 실제 다운로드 해보지 않았지만 모듈식 구조상 그렇습니다.)

하지만 **전부가 필요한 건 아닙니다**. 프로젝트의 목적(IFRS S1/S2 전환 에이전트, 특히 기후 리스크·배출량·재무 영향 중심)이 **기후 관련(GRI → IFRS S2 변환)**에 초점이 맞춰져 있으므로, ZIP 전체를 받을 필요 없이 **핵심 파일만 골라 받는 게 훨씬 효율적**입니다. 아래에서 정리해드릴게요.

### Full set ZIP에 포함되는 내용 요약 (2025년 6월 업데이트 기준)
- **Universal Standards** (모든 조직 공통, 3개): GRI 1 Foundation 2021, GRI 2 General Disclosures 2021, GRI 3 Material Topics 2021 + Glossary.
- **Sector Standards** (산업별, 4~5개): Oil & Gas (GRI 11), Coal (GRI 12), Agriculture/Fishing (GRI 13), Mining (GRI 14) 등 – 대부분 특정 산업에만 해당.
- **Topic Standards** (주제별, 30~40개 이상): 경제·환경·사회 전반.
  - 환경 관련: GRI 101 Biodiversity 2024, **GRI 102: Climate Change 2025** (최신 기후 변화 표준), **GRI 103: Energy 2025** (에너지), GRI 305 Emissions 2016 (온실가스 배출 – 여전히 핵심), GRI 302 Energy 2016, GRI 306 Waste 등.
  - 사회/경제: GRI 201~418 시리즈 (인권, 노동, 공급망 등).
- **기타**: Glossary (용어 사전), Basis for Conclusions 등.

→ 총 44개 이상의 PDF 파일이 들어가며, 대부분 사회·경제 주제나 비기후 산업 섹터는 당신의 프로젝트(기후 리스크 전환)에 직접적으로 필요하지 않습니다.

### 프로젝트에 정말 필요한 파일만 골라 받기 (추천 우선순위)
기후·배출·에너지 중심으로 IFRS S2(기후 리스크, 전환 위험, metrics)와 매핑하기 좋은 파일들입니다. 이 4~6개만 받으면 90% 이상 커버됩니다.

1. **GRI 102: Climate Change 2025** (Publ. 26 Jun 2025)  
   → **가장 중요!** 기후 변화 적응·완화, 전환 계획(transition plans), 시나리오 분석, GHG 목표 등 IFRS S2와 직접 매핑되는 내용. Gen Node 학습 데이터로 "GRI → IFRS 문체 변환"에 최적.

2. **GRI 103: Energy 2025** (Publ. 26 Jun 2025)  
   → 에너지 소비·재생에너지 전환. 기존 GRI 302를 대체하는 최신 버전.

3. **GRI 305: Emissions 2016**  
   → Scope 1/2/3 배출량 정의·측정·공시. 여전히 GRI의 배출 핵심 표준 (2025년에도 유효).

4. **GRI 302: Energy 2016** (필요 시)  
   → 에너지 관련 기본 지표 (GRI 103이 대체하지만, 이전 보고서 비교용).

5. **Universal Standards 기본 3개** (거버넌스·물질성 평가용):  
   - GRI 1: Foundation 2021  
   - GRI 2: General Disclosures 2021  
   - GRI 3: Material Topics 2021  
   → IFRS S1/S2의 Governance/Strategy 섹션과 연계.

6. **Glossary** (용어 사전)  
   → 임베딩 모델 튜닝 시 ESG 용어 Positive/Negative Pair 생성에 유용.

### 다운로드 전략
- **페이지에서 개별 다운로드**: https://www.globalreporting.org/how-to-use-the-gri-standards/gri-standards-english-language/ 에서 위 파일들만 "Show More >" 클릭 → PDF 직접 다운로드 (ZIP 안 받음).
- **ZIP 전체 받는 경우**: 파일이 너무 많아 관리 부담 → RAG Node에서 chunking/필터링 로직을 강화해야 함. (예: code_execution으로 unzip 후 "climate" 또는 "emissions" 키워드 포함 파일만 추출.)
- **파일 크기/구조 팁**: ZIP 풀면 폴더 구조(Universal / Sector / Topic)로 정리되어 있을 가능성 높음. 필요 없는 사회 주제 폴더는 무시 가능.

### 결론
- **전체 ZIP 필요성**: No – 방대하고 대부분 불필요. (기후 특화 프로젝트라 80~90% 파일은 쓸모없음)
- **대신**: 위 5~6개 PDF만 받으세요. 특히 **GRI 102 Climate Change 2025**와 **GRI 103 Energy 2025**는 2025년 신규라 IFRS S2 전환에 가장 잘 맞아요.
- **다음 단계**: 이 파일들 다운로드 후 PDF 파싱 (browse_pdf_attachment나 code_execution으로 키워드 추출) → RAG Node에 업로드 → Hybrid Search 테스트.

특정 파일의 직접 다운로드 링크(예: GRI 102 PDF)가 필요하시면 더 찾아드릴게요!