
## MCP 연결: Streamable HTTP vs stdio

### 1. stdio 방식

**개념**  
- MCP **클라이언트**(에이전트/API 프로세스)가 **서버를 subprocess로 실행**하고, 그 프로세스의 **표준 입출력(stdin/stdout)** 으로 JSON-RPC 메시지를 주고받는 방식입니다.  
- 서버 = 별도 Python 프로세스(`python sr_index_tools_server.py`), 클라이언트가 `command` + `args`로 매번 새로 띄웁니다.  
- 한 번의 “MCP 세션” = 한 subprocess의 수명과 같습니다.

**장점**  
- 구현이 단순하고, MCP/도구 서버를 **별도 서비스로 배포할 필요 없음**.  
- 네트워크/방화벽 설정 없이 **로컬에서만** 동작.  
- 클라이언트와 서버가 **같은 머신**이면 지연이 적음.

**단점**  
- **요청마다(또는 세션마다) subprocess를 새로 띄우면** 서버 기동(import, 초기화) 시간이 **첫 호출에 그대로 포함**됩니다.  
  → 첫 도구 호출이 90초 타임아웃처럼 보이는 원인이 됨.  
- 서버 프로세스가 **요청 사이에 재사용되지 않으면** 매번 콜드스타트.  
- **원격/다른 머신**에 있는 MCP 서버에는 쓰기 어렵고, **한 프로세스당 하나의 클라이언트** 연결에 가깝게 동작합니다.

---

### 2. Streamable HTTP 방식

**개념**  
- MCP 서버가 **HTTP 서버**로 떠 있고, 클라이언트는 **HTTP 요청**으로 같은 스펙(JSON-RPC 등)의 메시지를 보냅니다.  
- “Streamable”은 스펙상 **한 HTTP 연결/요청 안에서 양방향 스트리밍**을 지원하는 전송 방식을 말합니다.  
- 서버 = **항상 떠 있는 프로세스**(예: `MCP_HTTP=1`로 `sr_index_tools_server.py`를 한 번 실행해 두고, `http://host:port/mcp`에서 대기).  
- 클라이언트 = **URL만 알면** 그 서버에 연결합니다. subprocess를 띄우지 않습니다.

**장점**  
- **서버를 한 번만 띄워 두면** 되므로 **첫 요청부터** 콜드스타트 없이 도구 호출 가능.  
- **여러 클라이언트**가 같은 서버 URL로 동시에 붙을 수 있어 **공유 인프라**로 쓰기 좋습니다.  
- **다른 호스트/컨테이너**에 MCP 서버를 두고, 클라이언트는 URL만 바꿔서 연결 가능(분산 구성).  
- 방화벽/로드밸런서 등 **일반 HTTP 인프라**로 관리·모니터링하기 쉽습니다.

**단점**  
- 서버를 **별도 프로세스/서비스로** 띄워 두고 유지해야 함(운영 부담).  
- **네트워크**와 **인증**(헤더, 토큰 등) 설정이 필요할 수 있음.  
- 클라이언트/서버가 **같은 머신**이어도 HTTP 한 번 타고 가므로, stdio보다는 약간의 오버헤드가 있을 수 있음(보통 미미).

---

### 3. 개념 비교 요약

| 구분           | stdio                                             | Streamable HTTP                   |
| ------------ | ------------------------------------------------- | --------------------------------- |
| **연결 형태**    | 클라이언트가 서버 프로세스를 subprocess로 실행, stdin/stdout으로 통신 | 서버는 미리 떠 있고, 클라이언트가 HTTP로 URL에 연결 |
| **서버 수명**    | 클라이언트가 띄운 subprocess 수명에 종속                       | 서버 프로세스가 독립적으로 상시 구동              |
| **첫 호출**     | 서버 기동 시간이 포함될 수 있음(콜드스타트)                         | 이미 떠 있으므로 첫 호출도 빠름                |
| **다중 클라이언트** | 보통 세션/프로세스당 1:1                                   | 하나의 서버에 여러 클라이언트 연결 가능            |
| **원격 배치**    | 같은 머신 위주                                          | 다른 호스트/포트에 서버 두기 적합               |
| **운영**       | 별도 “서버 서비스” 없이 클라이언트만 있어도 됨                       | 서버 프로세스/포트 관리 필요                  |


---

### 4. 이 코드베이스에서의 사용

- **stdio**: `MCP_SR_INDEX_TOOLS_URL` 등을 **설정하지 않으면** 기존처럼 subprocess + stdin/stdout으로 MCP 툴 서버에 연결합니다.  
- **Streamable HTTP**: `MCP_HTTP=1`로 툴 서버를 HTTP 모드로 띄우고, 클라이언트(API)에 `MCP_SR_INDEX_TOOLS_URL=http://127.0.0.1:8000/mcp` 를 주면, **subprocess 없이** 해당 URL로만 연결합니다.

정리하면, **stdio = “클라이언트가 서버를 매번 실행하고 파이프로 대화”**, **Streamable HTTP = “서버는 이미 떠 있고, 클라이언트가 HTTP로 대화”** 라고 보면 됩니다.





















## 1. DB 저장이 `sr_workflow.py`로 “통일”되어 있나?

**완전히 통일된 것은 아닙니다.**

- **LangGraph `extract-and-save/*` 흐름**에서는 메타·인덱스·본문·이미지 저장이 **`sr_workflow.py`의 각 `_save_*_node`** 안에서 이뤄지고, 실제 INSERT는 대부분 **`backend/domain/shared/tool/sr_report/save/sr_save_tools.py`** 의 `save_historical_sr_report`, `save_sr_report_index_batch`, `save_sr_report_body_batch` 등을 **`invoke` / `asyncio.to_thread`** 로 호출하는 형태입니다.

- 다만 **같은 저장 함수들**이  
  - 레거시 **`SROrchestrator._execute_legacy` → `sr_save_agent`**,  
  - 또는 **허브 `repositories/*`** (`staging_repository`, `historical_sr_report_repository`, `sr_parsing_result_repository` 등)  
  처럼 **워크플로 밖에서 세션을 열고 `commit`하는 경로**도 있습니다.

정리하면: **“SR 파이프라인의 권장 저장 진입점은 LangGraph `sr_workflow`”**에 가깝고, **“모든 DB 쓰기가 오직 `sr_workflow` 한 파일로만 간다”**는 뜻은 **아닙니다**. 구현은 **`sr_save_tools` + 여러 진입점**으로 나뉘어 있습니다.

---

## 2. 세 파일 역할

### `sr_llm_review.py`

- **역할**: SR 저장 **전/중**에 **LLM으로 검토·보정·매핑**을 하는 **허브 유틸** 모음.
- **주요 내용** (파일 상단·함수 기준):
  - **`review_sr_metadata_with_llm`**: `historical_sr_reports`용 메타 dict 검토·보정 (`save_metadata` 노드에서 사용).
  - **`map_page_markdown_to_sr_report_index`**: 페이지별 마크다운 → **`sr_report_index` 행 리스트**로 만드는 **LLM 매핑** (`sr_index_agent`에서 LlamaParse 결과만 있을 때 등).
  - **`review_sr_index_rows_with_llm`**: 인덱스 행 배열 전체를 LLM이 재검토 (코드상 정의는 있으나, **다른 모듈에서 호출이 연결돼 있지 않으면** 실제 플로우에는 안 탈 수 있음).
  - **`detect_sr_index_anomalies` / `correct_anomalous_index_rows_with_md`**: 이상치 탐지·마크다운 기반 보정 등.
  - 스키마 설명 상수 **`SR_INDEX_SCHEMA_CONTEXT`**, 행 병합 **`merge_corrected_index_rows`** 등.
- **DB 직접 저장 책임은 없음** — “값을 다듬어 다음 단계에 넘기는” 쪽에 가깝습니다.

---

### `sr_index_plain_text.py`

- **역할**: 인덱스가 **마크다운 파이프 표가 아니라**, 스페이스로 맞춘 **평문·다단(2열) 레이아웃**일 때의 **전처리·휴리스틱**.
- **주요 내용**:
  - **`SR_PLAIN_TEXT_INDEX_RULES`** 등: LLM 프롬프트에 넣는 **규칙 문구** (GRI 다단, 왼쪽/오른쪽 열 모두 추출하라는 지시 등).
  - **`prepare_index_page_markdown_for_llm`** 등: 줄 앞에 **`NNNN|`** 같은 **줄 번호 붙이기**, 네비게이션 줄 걸러내기, **오른쪽 열 2차 패스**용 자르기 등.
  - GRI/ESRS/IFRS **본문 vs 네비** 구분용 정규식 (`_GRI_STANDARDS_INDEX_HINT` 등).
- **의도**: `sr_llm_review.map_page_markdown_to_sr_report_index` 같은 LLM 호출 **전에** 텍스트를 정리해 **추출 품질을 올리는** 층. **DB 저장과 무관**.

---

### `sr_index_page_remap.py`

- **역할**: 인덱스만 잘라 만든 **소형 PDF(슬라이스)** 로 파싱했을 때, 셀에 적힌 페이지 번호가 **슬라이스 기준 1~N**인지 **원본 PDF 절대 페이지**인지 섞일 수 있어서, **원본 보고서 페이지 번호로 맞추는** 순수 함수 모듈.
- **함수**:
  - **`remap_slice_pages_to_original`**: `page_numbers` 리스트를 `chosen_pages`(슬라이스에 쓴 원본 페이지 목록)와 `total_pages`로 보정.
  - **`remap_index_page_number_to_original`**: `index_page_number` 한 칸에 대해 동일하게 보정.
- **`sr_index_agent`의 `_apply_best_parsing_result`** 병합·페이지 보정 단계에서 사용된다고 이전에 본 흐름과 일치합니다. **DB 저장 없음**, **숫자 좌표만 정리**.

---

요약하면: **저장 단일화**는 `sr_workflow`에 **가깝지만 전역 통일은 아니고**, 세 파일은 각각 **LLM 검토/매핑(`sr_llm_review`)**, **평문 인덱스 전처리(`sr_index_plain_text`)**, **페이지 번호 리맵(`sr_index_page_remap`)** 역할로 나뉩니다.














네 가지 모두 **큰 방향은 맞고**, 아래처럼만 보정하면 정확합니다.

---

### 1. `sr_workflow.py` 진입점 + `sr_save_tools` 실제 저장?

**맞습니다.** LangGraph `extract-and-save` 계열에서는 **`sr_workflow.py`의 `_save_*_node`가 저장 단계의 진입점**이고, **실제 INSERT는 `sr_save_tools`의 LangChain Tool**을 씁니다.

- **메타**: `_save_metadata_node`에서 `save_historical_sr_report.invoke`를 `asyncio.to_thread`로 호출합니다.  
  ```104:115:backend/domain/v1/data_integration/hub/orchestrator/sr_workflow.py
    report_id = await asyncio.to_thread(
        save_historical_sr_report.invoke,
        {
            "company_id": meta.get("company_id"),
            ...
        },
    )
  ```
- **인덱스**: `_save_index_node`에서 에이전트가 만든 `sr_report_index`를 받은 뒤 `save_sr_report_index_batch.invoke`를 호출합니다.  
  ```180:183:backend/domain/v1/data_integration/hub/orchestrator/sr_workflow.py
    save_result = await asyncio.to_thread(
        save_sr_report_index_batch.invoke,
        {"report_id": report_id, "indices": sr_report_index},
    )
  ```
- **본문·이미지**: 워크플로우는 **`sr_body_agent` / `sr_images_agent`에만 위임**하고, `saved_count` 등을 받습니다. **`save_sr_report_body_batch`는 이 파일에서 직접 부르지 않고**, 에이전트 쪽에서 `sr_save_tools`를 쓰는 구조입니다(로그에 나온 `save_sr_report_body_batch`와 동일 계열).

정리: **“워크플로가 단계를 조율하고, 저장은 `sr_save_tools` 툴로 간다”**는 이해가 맞고, **본문/이미지는 워크플로 → 라우터 → 에이전트 → save 툴** 순입니다.

---

### 2. `sr_llm_review.py` = 마크다운 → `sr_report_index` 스키마로 LLM 추출·보정?

**대체로 맞습니다.** 다만 **역할이 인덱스만은 아닙니다.**

- **`map_page_markdown_to_sr_report_index`**: 페이지별 마크다운 → **`sr_report_index` 행 리스트**로 LLM 매핑(스키마 설명은 `SR_INDEX_SCHEMA_CONTEXT`).  
  ```510:523:backend/domain/v1/data_integration/hub/sr_llm_review.py
async def map_page_markdown_to_sr_report_index(
    page_markdown: Dict[Any, Any],
    ...
) -> List[Dict[str, Any]]:
    """
    LlamaParse가 준 page_markdown만 있을 때, LLM으로 인덱스 표를 sr_report_index 행 리스트로 채웁니다.
  ```
- **`review_sr_metadata_with_llm`**: 메타 dict 검토·보정(메타 저장 전).
- 그 외 이상치·보정 헬퍼 등.

즉 **“인덱스용 MD → 스키마 맞춤 추출/보정”은 핵심 중 하나**이고, **메타 검토**도 같은 파일에 있습니다.

---

### 3. `sr_index_plain_text.py` = `_all_pages.md` 같은 파일 전용 전처리?

**반만 맞습니다.** 이 모듈은 **“디스크에 있는 특정 경로의 `_all_pages.md`만”** 다루는 게 아니라, **어디서 오든 LLM에 넘기기 전 문자열**에 대해 **`prepare_index_page_markdown_for_llm`** 등으로 줄 번호 붙이기·다단/평문 휴리스틱을 적용합니다.  
`sr_llm_review.map_page_markdown_to_sr_report_index` 안에서 **`prepare_index_page_markdown_for_llm(str(md).strip())`** 로 호출됩니다.  
  ```542:548:backend/domain/v1/data_integration/hub/sr_llm_review.py
        ...
        text = prepare_index_page_markdown_for_llm(str(md).strip())
        chunks = split_markdown_index_chunks(text, max_chars=8000, overlap=400)
  ```

`_all_pages.md`는 **디버그/캐시로 남긴 산출물 예시**일 수 있고, 파이프라인상으로는 **“페이지별 마크다운 dict”**가 들어올 때 같은 전처리가 붙습니다. **“일반적인 깔끔한 표 MD가 아니라, 평문·다단형 인덱스일 때 품질 올리는 층”**이라고 보면 됩니다.

---

### 4. `sr_index_page_remap.py` = 슬라이스 페이지 → 원본 보고서 페이지?

**맞습니다.** `remap_slice_pages_to_original` 등으로 **인덱스용으로 잘라 파싱했을 때 1…N으로 나온 번호를, 원본 PDF 절대 페이지로 맞추는** 용도이고, **`sr_index_agent` 병합 단계에서** `page_numbers` 등에 적용됩니다(이미 grep으로 확인).

---

요약: **1·2·4는 이해 그대로 쓰셔도 되고**, **3만** “특정 `_all_pages.md` 파일 전용”이 아니라 **“LLM에 넘기는 인덱스 페이지 마크다운 문자열 전반에 대한 전처리 유틸”**로 보시면 됩니다.















질문을 나눠서만 짚겠습니다.

---

### 1. 본문/이미지는 `sr_workflow`를 안 거치고 저장 툴만 쓰나?

**아니요가 기본입니다.** 공식 `extract-and-save` 경로는 **그래프를 탑니다.**

- **`POST .../extract-and-save/body`**: `get_sr_graph().ainvoke` → **`_save_body_node`** → `AgentRouter` → **`sr_body_agent`** → 그 안에서 `save_sr_report_body_batch` 호출.
- **`POST .../extract-and-save/images`**: 마찬가지로 그래프 → **`_save_images_node`** → **`sr_images_agent`** (저장은 에이전트 쪽).

즉 **저장 툴을 `sr_workflow.py`가 직접 부르는 게 아니라**, **워크플로가 에이전트를 호출하고, 에이전트가 저장 툴을 부르는** 구조입니다.

**예외**: **`POST .../extract-and-save/body-agentic`** 은 주석대로 **LangGraph 없이** `SRBodyAgent().execute()`만 호출합니다. 이때는 **`sr_workflow`를 거치지 않지만**, 저장은 여전히 **`sr_body_agent` 안에서 `save_sr_report_body_batch`** 로 이뤄집니다.

정리: **“워크플로를 건너뛴다”는 건 `body-agentic` 같은 별 엔드포인트일 때이고**, **저장 툴을 API가 직접 부르는 것도 아닙니다** (에이전트가 부름).

---

### 2. (이전 답) 유지 — `sr_llm_review`는 인덱스 MD 매핑 + 메타 등

추가로 할 말 없음.

---

### 3. LlamaParse만 취급? Docling은 행이 있으면 안 하나?

**`prepare_index_page_markdown_for_llm` 이 돌아가는 곳은 `map_page_markdown_to_sr_report_index` 안**이고, 이 함수는 **`sr_index_agent._ensure_llamaparse_rows_from_markdown` 에서만** 이렇게 쓰입니다:

```472:478:backend/domain/v1/data_integration/spokes/agents/sr_index_agent.py
        LlamaParse가 sr_report_index 없이 page_markdown만 줄 때, LLM으로 행 리스트를 채웁니다.
        이미 행이 있으면 그대로 둡니다.
        """
        sr = llamaparse_result.get("sr_report_index") or []
        if len(sr) > 0:
            return llamaparse_result
```

- **LlamaParse 도구 결과에 이미 `sr_report_index` 행이 있으면** → LLM 매핑 **`prepare_index_page_markdown` 포함 전부 스킵**.
- **행이 비어 있고 `page_markdown`만 있으면** → 그때 **`map_page_markdown_to_sr_report_index`** (그 안에서 `prepare_index_page_markdown_for_llm`) 호출.

**Docling**은 보통 **`parse_index_with_docling_tool`이 곧바로 `sr_report_index`를 주는 경로**라서, **그 텍스트에 대해 `prepare_index_page_markdown`을 쓰지 않습니다.**  
Docling과 LlamaParse는 **병합**되지만, **“마크다운 → 행” LLM 파이프는 LlamaParse 쪽 `page_markdown`이 필요할 때만** 탑니다.

그래서 **“LlamaParse 산출물만 이 전처리·LLM 매핑 대상”**에 가깝고, **“Docling은 이미 표가 구조화돼 있으면 그 경로를 안 탄다”**는 이해가 맞습니다.

---

### 4. `prepare_index_page_markdown_for_llm` = LLM 전 **모든** 전처리?

**전부는 아닙니다.** 같은 함수 `map_page_markdown_to_sr_report_index` 안에서:

- `prepare_index_page_markdown_for_llm` 후에 **`split_markdown_index_chunks`**, **`build_llm_index_context_prefix`** 등이 이어지고,
- 평문 다단이면 **`build_right_column_plaintext_supplement`** 로 **두 번째 패스**도 합니다.

즉 **`sr_index_plain_text`의 일부 함수가 “LLM 직전 1차 정리”**에 가깝고, **청크 분할·컨텍스트 접두·오른쪽 열 보충**은 **같은 파일/모듈의 다른 단계**입니다.  
**“LLM으로 넘기기 전 파이프라인 전체를 한 함수가 담당”은 아닙니다.**









| 컬럼                       | 설명                                                                                                                                        |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| id                       | 통합 행의 고유 식별자(PK).                                                                                                                         |
| company_id               | 데이터가 속한 회사. `companies(id)` FK.                                                                                                           |
| period_year              | 보고·집계 연도(필수).                                                                                                                             |
| period_month             | 월 단위일 때 사용. NULL이면 연간으로 해석.                                                                                                               |
| source_entity_type       | 값이 온 원본 도메인/테이블 종류. 허용값: `environmental`, `social`, `governance`, `company_info`, `content`, `chart` (각각 environmental_data 등 6개 소스와 대응). |
| source_entity_id         | 원본 테이블 행의 PK. `source_entity_type`과 짝을 이루는 다형(polymorphic) 참조로 “어디서 복사·집계됐는지” 추적.                                                         |
| unified_column_id        | `unified_column_mappings`의 통합 컬럼 ID(FK). 기준서별 DP 매핑(`mapped_dp_ids` 등)과 연결되는 의미 단위 키.                                                     |
| data_value               | 실제 내용을 JSONB로 저장. 수치·문장 등 형태가 달라도 한 컬럼으로 담기 위해 유연하게 둠(예: `{"value": ...}`, `{"text": ...}`).                                              |
| data_type                | 값의 성격. `quantitative`(정량), `qualitative`(정성), `narrative`(서술), `binary`. CHECK로 제한.                                                       |
| unit                     | 정량일 때 단위(예: tCO2e, 명). 비정량이면 보통 NULL.                                                                                                     |
| data_source              | 데이터 출처 라벨(예: `ghg_emission_results`, `hr`, `manual`). 시스템·파이프라인 구분용.                                                                      |
| calculation_method       | 특히 GHG 등 산정 방법 메모.                                                                                                                        |
| confidence_score         | 데이터 신뢰도 0~100 스케일(문서상 DECIMAL(5,2)).                                                                                                      |
| status                   | 워크플로 상태. 기본 `draft`. 예: `pending_review`, `approved`, `final_approved`.                                                                   |
| approved_by              | 승인한 사용자. `users(id)` FK.                                                                                                                  |
| approved_at              | 승인 시각.                                                                                                                                    |
| included_in_final_report | 최종 SR에 포함될 통합 행인지. 조회·필터(기준서별 문단 생성 등)에 자주 쓰일 플래그.                                                                                        |
| final_report_version     | 최종본 버전 태그(예: `v1`, `v2`).                                                                                                                 |
| created_at / updated_at  | 생성·수정 시각.                                                                                                                                 |













공개 102-4 온실가스 배출 감축 목표 및 진행 상황

요구 사항

지침

조직은 다음을 수행해야 합니다:

a.
단기, 중기 및 장기 총 범위 1, 범위 2 및 범위 3 온실가스 배출 감축 목표를 이산화탄소 환산 톤(CO₂e) 단위 및 기준 연도 배출량의 백분율로 보고해야 하며, 여기서:

i.총 범위 1, 범위 2 및 범위 3 온실가스 배출 감축 목표는 별도로 보고되거나 범위 1 및 범위 2 온실가스 배출이 합산된 경우로 구분됩니다;

ii.총 스코프 1 및 스코프 2 온실가스 배출 감축 목표는 공개항목 102-5 및 102-6에 보고된 총 스코프 1 및 스코프 2 온실가스배출량을 포함합니다;

iii.온실가스 흡수량, 온실가스 거래량 및 회피된 온실가스 배출량은 제외됩니다;

b.각 총 온실가스 배출 감축 목표에 대해, 생물 기원 CO₂ 배출량이 목표에 포함되는지 여부를 보고합니다;

c.각 총 범위 2 온실가스 배출 감축 목표에 대해, 목표가 위치 기반 방식 또는 시장 기반 방식을 사용하는지 여부를 보고하십시오;

d.각 총 범위 3 온실가스 배출 감축 목표에 대해, 해당 목표가 적용되는 범위 3 범주를 열거하십시오;

e.각 온실가스 총 배출량 감축 목표에 대해, 해당 목표가 적용되는 가스를 보고하십시오;

f.온실가스 총 배출량 감축 목표가 지구 온난화를 1.5°C로 제한하는 데 필요한 노력에 관한 최신 과학적 증거와 어떻게 부합하는지 설명하십시오;

g.총 온실가스 배출 감축 목표 수정 정책을 기술하십시오;

h.각 온실가스 총 배출량 감축 목표에 대해 기준 연도를 보고하며, 다음을 포함한다:

i.기준 연도 선정 근거;

ii.기준 연도 배출량(CO₂ 환산 톤 단위);

iii.기준년도 배출량 재계산을 유발한 배출량의 중대한 변화 배경;

iv.기준 연도 배출량이 재계산되는 경우, 이전에 보고된 기준 연도 배출량;

i.각 온실가스 총 배출량 감축 목표에 대한 진척 상황을 인벤토리 방법을 사용하여, 이산화탄소 환산 톤CO₂e단위 및 기준 연도 배출량의 백분율로 보고하십시오;

j.각 온실가스 총 배출량 감축 목표에 대해, 목표 달성 진척도가 어떻게 이루어졌는지 설명하고 그 원인이 다음 중 어느 것인지를명시할 것:

i.조직의 이니셔티브로 인한 감축; 또는

ii.기타 요인에 의한 것인지;

k.사용된 기준, 방법론, 가정 및 계산 도구를 보고하십시오.






본 공개에 따라 보고된 온실가스 배출 감축 목표는 102-1-f에 따른 전환 계획 달성을 위한 목표를 보고하는 데 사용됩니다.

102-4-a에 대한 지침

조직은 목표가 적용되는 범위 3 범주와 공개 102-7이 적용되는 범위 3 범주 간 일관성을 확보해야 합니다.

조직은 단기, 중기 및 장기 목표의 기간을 어떻게 정의했는지 보고해야 합니다. 조직이 단기, 중기 및 장기 목표를 정의할 수 있는 예시

는 다음과 같습니다:

• 기준 연도로부터 5~10년의 단기 목표, 기준 연도로부터 10~15년의 중기 목표, 기준 연도로부터 20~30년(예: 2050년까지)의

장기 목표.

• 기준 연도로부터 1~2년의 단기 목표, 기준 연도로부터 3~5년의 중기 목표, 기준 연도로부터 10년의 장기 목표.

단기, 중기, 장기 시간 범위는 조직마다 다를 수 있으며 산업별 특성을 포함한 여러 요인에 따라 달라집니다. 조직은 또한 목표가 설정된

연도를 보고해야 합니다. 단기, 중기, 장기 목표에 대한 자세한 내용은 참고문헌 [12]를 참조하십시오.

중대한 변화로 인해 기존 온실가스 배출 감축 목표의 적절성과 일관성이 훼손될 경우, 조직은 해당 변화를 반영하여 목표를 재계산해

야 합니다. 조직은 GRI 2: 일반 공시 2021의 공시 2-4에 따라 정보 재공시를 보고해야 합니다.

범위 1, 범위 2, 범위 3 온실가스 배출 감축 목표 보고 외에도 조직은 집약도 목표를 보고할 수 있습니다. 집약도 목표는 범위 1, 범위

2, 범위 3별로 별도로 보고해야 합니다.

102-4-a-i 지침

조직은 범위 1, 범위 2, 범위 3 온실가스 배출량을 포함한 통합 온실가스 배출 감축 목표를 보고할 수도 있습니다. 이 경우 조직은 해당

정보가 관련성이 있는 이유(예: 조직의 업종 내에서)를 설명해야 합니다.

통합 온실가스 배출 감축 목표를 보고할 경우, 조직은 목표에 포함된 총 온실가스 배출량 대비 각 범위가 차지하는 비율을 보고해야 합

니다.

102-4-a-ii에 대한 지침

조직이 공개항목 102-5 및 102-6에 따라 보고된 총 스코프 1 및 스코프 2 온실가스 배출량을 모두 포함하지 않는 스코프 1 및 스코프

2 온실가스 배출량 감축 목표를 보고하는 경우, 그 이유를 설명해야 합니다. 또한 해당 목표가 총 스코프 1 및 스코프 2 온실가스 배출

량에서 차지하는 비율을 보고하고, 총량을 포함하기 위한 일정과 단계를 제시해야 합니다.

102-4-a-iii에 대한 지침

온실가스 제거량, 온실가스 거래(탄소 크레딧 포함), 회피된 온실가스 배출량은 102-4-에 따라 보고된 조직의 총 온실가스 배출량 감축

목표에서 제외됩니다.

a에 따라 보고되는 조직의 총 온실가스 배출 감축 목표에서 제외됩니다. 온실가스 제거 및 탄소 크레딧 사용에 대한 자세한 내용은 102-9

c 및 102-10-d에 대한 지침을 참조하십시오.

회피된 온실가스 배출량은 기업 인벤토리와 별도의 회계 체계에 속하며, 온실가스 배출 감축 목표에 포함되지 않습니다.

순 온실가스 배출 감축 목표 설정이 허용되는 부문별 프로그램 적용 대상 기관은 온실가스 배출 감축 목표와 온실가스 제거량을 별도

로 보고해야 합니다. 이 경우 기관은 채택된 권위 있는 과학적 증거에 기반하여 부문별 프로그램을 보고해야 합니다. 추가 지침은 참고

문헌 [10]을 참조하십시오.

102-4-b에 대한 지침

과학 기반 목표 설정 이니셔티브는 각 총 온실가스 배출 감축 목표에 생물 기원 CO₂ 배출량을 포함하도록 요구합니다.

참고문헌 [11]을 참조하십시오.

102-4-c에 대한 지침

조직이 시장 기반 방법을 사용하여 범위 2 온실가스 배출 감축 목표를 보고하는 경우, 조직은 위치 기반 방법을 사용하여 범위 2 온실

가스 배출 감축 목표를 별도로 보고해야 합니다.

조직이 시장 기반 방법을 사용하여 범위 2 온실가스 배출 감축 목표를 설정할 경우, 범위 2 품질 기준이 사용된 계약적 수단에 적용됩

니다. 범위 2 품질 기준에 대한 자세한 내용은 102-6-a에 대한 지침을 참조하십시오.

102-4-d에 대한 지침

범위 3 온실가스 배출 목표가 모든 범위 3 범주를 포함하지 않는 경우, 조직은 목표가 포함하는 범위 3 배출량의 비율(102-7-a 및 102-7-c

에 보고됨)을 보고해야 합니다. 비율은 다음 공식을 사용하여 계산할 수 있습니다:

목표가 적용되는 범위 3 배출량

% =_________________________________________________________________________________________________100

총 범위 3 배출량 (102-7-a) + 생물 기원 범위 3 배출량 (102-7-c)

조직은 스코프 3 범주 중 제외된 항목이 있는 경우 그 이유를 설명하고, 향후 모든 범주를 포함하기 위해 취한 조치를 기술해야 합니다.

범위 3 온실가스 배출 범주에 대한 자세한 내용은 102-7-a에 대한 지침을 참조하십시오.

102-4-f에 대한 지침

조직은 온실가스 배출 감축 목표가 해당 산업별 과학 기반 경로와 일치하는지 여부와 그 방법을 보고해야 합니다.

조직은 목표를 결정하는 데 사용한 지침 또는 프레임워크(기후 및 정책 시나리오 포함)를 보고해야 합니다. 조직은 온실가스 배출 감축

목표를 설정할 때 미래 발전 상황(예: 판매량 변화, 합병 및 인수)과 전환 위험 및 기회(예: 소비자 행동 및 수요 변화, 강화된 규제 환경,

신기술)를 어떻게 고려했는지 설명해야 합니다. 또한 이러한 발전 상황과 위험이 목표 달성에 어떤 영향을 미칠 수 있는지 설명해야 합

니다.

102-4-g에 대한 지침

102-4-g를 보고할 때 조직은 온실가스 배출 감축 목표 업데이트 주기를 보고할 수 있습니다. 예를 들어, 조직은 온실가스 배출 감축 목

표를 5년마다 업데이트한다고 보고할 수 있습니다.

조직은 또한 온실가스 배출 감축 목표를 수정하게 된 주요 사유를 보고해야 합니다. 예를 들어:

• 이해관계자 요구(예: 고객, 투자자);

• 목표 수립에 활용된 시나리오의 변화;

• 목표 수립에 활용된 기준 또는 참고 자료의 변화;

• 변화하는 환경(예: 재생에너지 비용 변화);

• 기술적 돌파구(예: 새로운 생산 공정);

• 법률 변경;

• 목표 연도 이전에 목표 달성;

• 온실가스 배출량 계산 방법 개선.

102-4-h-i에 대한 지침

인벤토리(102-5, 102-6, 102-7 참조)와 목표 기준 연도(102-4 참조)에 서로 다른 연도를 사용할 수 있지만, 일반적으로 동일한 연도

를 사용하는 것이 더 간단합니다.

목표 기준 연도 선택에 대한 자세한 내용은 GHG 프로토콜 기업 회계 및 보고 표준[14]을 참조하십시오.

102-4-h-iii에 대한 지침

기준년도 배출량 재계산을 유발할 수 있는 사례는 다음과 같습니다:

• 기초 연도 배출량에 중대한 영향을 미치는 조직의 구조적 변화(합병, 인수, 매각, 배출 활동의 아웃소싱 및 인소싱 포함);

• 기초 연도 배출량 데이터에 중대한 영향을 미치는 배출계수 또는 활동 데이터의 정확도 개선 또는 계산 방법론 변경;

• 중대한 오류 또는 누적된 오류들이 종합적으로 중대한 영향을 미치는 경우. 이러한 경우 조직은 향후 보고에서 유사한 오류를 방지

하기 위한 확립된 절차도 보고해야 합니다.

102-4-i에 대한 지침

온실가스 배출 목표 달성 현황을 보고할 때, 온실가스 제거량, 온실가스 거래량 및 회피된 온실가스 배출량은 제외됩니다.

온실가스 배출 목표 달성 현황은 온실가스 배출량의 감소 또는 증가를 포함한다.

인벤토리 방식은 배출량을 기준 연도와 비교합니다. 인벤토리 방식을 사용한 목표 달성 진척도는 다음 공식으로 계산됩니다:

배출량 변화 = 당해 연도 배출량 – 기준 연도 배출량

인벤토리 방법에 대한 자세한 내용은 GHG 프로토콜 기업 회계 및 보고 표준에서 확인할 수 있습니다.

기준 연도 배출량의 백분율로 표시된 목표 달성률은 다음 공식으로 계산됩니다:

진전률 = (배출량 변화 / 기준년도 배출량)*100

진척도는 백분율로 보고할 수 있으며, 다음 예시와 같습니다: 스코프 1 및 스코프 2 온실가스 배출량은 2019년 기준 연도 대비 20% 감소

했습니다.

공개 102-4의 요구사항 정보를 제시하는 방법의 예는 부록의 표 3을 참조하십시오.

온실가스 배출 감축 목표 달성 현황을 보고할 때, 조직은 목표 달성에 알려진 장애 요인과 해당되는 경우 고정된 온실가스 배출량의 역

할을 설명해야 합니다. 고정된 온실가스 배출량에 대한 자세한 내용은 102-1-f에 대한 지침을 참조하십시오.

102-4-j에 대한 지침

온실가스 배출 감축 목표에 대한 진전은 조직의 이니셔티브를 통해서 또는 다른 효과나 요인으로 인한 배출량의 변화를 통해서 달성될

수 있습니다.

조직의 온실가스 배출 감축을 위한 주요 활동은 다음과 같습니다:

• 공정 재설계;

• 장비 전환 및 개조;

• 연료 전환;

• 행동 변화.

온실가스 배출 감축을 초래하는 기타 효과 또는 요인으로는 다음이 포함될 수 있습니다:

• 정부 정책에 의한 전력망 탈탄소화;

• 공급업체가 주도하는 구매 제품 및 서비스의 탈탄소화;

• 정부 폐기물 정책으로 인한 폐기물 처리 배출량 감소;

• 소비자 행동 변화(예: 운전 감소).

102-4-k 지침

조직은 독립적인 제3자가 온실가스 배출 감축 목표 및 관련 진척 상황을 검증했는지 여부를 보고해야 하며, 검증한 경우 해당

검증을 수행한 기관과 사용된 기준 또는 방법론을 명시해야 합니다.<--이 데이터로 생성한 datapoint와 rulebook에


{

    "status": "success",

    "pipeline": "ucm_nearest_v1",

    "dry_run": true,

    "stats": {

        "processed": 40,

        "accept": 0,

        "review": 0,

        "reject": 40,

        "upsert_ok": 0,

        "upsert_error": 0,

        "errors": 40

    },

    "items": [

        {

            "source_dp_id": "GRI2-2-9-c-ii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-9-c-iii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-9-c-iv",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-b",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-c",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-5-a",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-d",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-d-i",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-d-ii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-d-iii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-e",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-f",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-40-g",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-41",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-42",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-42-b",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-6-b-iii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-9-c-v",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-42-c",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-9-c-vi",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-AR12",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-AR13-a",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-AR13-b",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-AR14",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-AR14-a",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-9-c-vii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "S2-25-a-vi",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-42-a",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-9-c-viii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-10",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-10-a",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-10-b",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-10-b-i",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-10-b-iii",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-10-b-iv",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-11",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-11-b",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "GRI2-2-12",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-SBM-1-AR13",

            "status": "error",

            "message": "no candidates"

        },

        {

            "source_dp_id": "ESRS2-MDR-T-80-d",

            "status": "error",

            "message": "no candidates"

        }

    ]

}



