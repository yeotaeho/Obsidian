# Obsidian → Velog 변환 가이드

이 저장소는 Obsidian 볼트의 개발 노트를 Velog 블로그용 마크다운으로 변환하는 작업 공간이다.
**이번 단계에서는 텍스트만 변환한다. 이미지는 처리하지 않는다.**

## 작업 흐름

1. 원본 노트는 `source/`(또는 사용자가 지정한 볼트 경로)에서 읽는다.
2. 변환 결과는 `staging/`에 동일한 파일명의 `.md`로 저장한다. 원본은 절대 수정하지 않는다.
3. 변환 후 사용자가 `staging/`의 결과물을 직접 검토한다. 발행은 별도 단계다.

## 변환 규칙

### 1. 위키링크 `[[...]]`

Velog는 위키링크를 지원하지 않으므로 모두 일반 텍스트나 링크로 변환한다.

- `[[노트제목]]` → `노트제목` (대상 노트가 함께 발행되지 않으면 일반 텍스트로)
- `[[노트제목|표시텍스트]]` → `표시텍스트`
- `[[노트제목#헤딩]]` → `노트제목` (헤딩 앵커는 제거)
- 외부 URL을 가리키는 일반 마크다운 링크 `[텍스트](url)`는 그대로 둔다.

판단이 애매하면 깨진 링크보다 일반 텍스트로 남기는 쪽을 택한다.

### 2. 이미지 임베드 (이번 단계에서는 보류)

- `![[이미지.png]]` 또는 `![](로컬경로.png)` 형태의 **로컬 이미지 임베드**는 변환하지 않고 그대로 둔다.
- 단, 해당 라인 바로 위에 `<!-- TODO: 이미지 업로드 필요 -->` 주석을 삽입해 나중에 찾기 쉽게 표시한다.
- 이미 외부 URL(`http`/`https`)을 가리키는 이미지는 그대로 둔다.

### 3. 콜아웃 (Callout)

Velog는 Obsidian 콜아웃을 지원하지 않는다. 인용구 + 이모지로 변환한다.

- `> [!note]` / `> [!info]` → `> ℹ️ **참고**`
- `> [!tip]` → `> 💡 **팁**`
- `> [!warning]` / `> [!caution]` → `> ⚠️ **주의**`
- `> [!danger]` / `> [!error]` → `> 🚨 **경고**`
- `> [!quote]` → 일반 인용구 `>`로
- 그 외 타입 → `> 📌 **<타입명>**`
- 콜아웃 내부 본문 줄의 `>` 인용 형식은 유지한다.

### 4. Frontmatter (YAML)

Obsidian frontmatter는 Velog 발행 메타데이터로 매핑할 정보만 남기고 정리한다.

- `tags`: 배열을 그대로 유지한다 (발행 시 Velog 태그로 사용).
- `title`: 있으면 유지. 없으면 파일명이나 첫 H1을 제목 후보로 본다.
- `series`: 있으면 유지 (Velog 시리즈로 사용).
- Obsidian 전용 필드(`aliases`, `cssclass`, `created`, `updated`, `id` 등 변환에 불필요한 것)는 제거한다.
- frontmatter가 없으면 만들지 않는다.

### 5. 기타 Obsidian 문법

- `==하이라이트==` → Velog는 미지원이므로 `**하이라이트**`(굵게)로 변환한다.
- 각주 `[^1]` 문법은 표준 마크다운이므로 그대로 둔다.
- 태그 `#태그`(본문 중간의 인라인 태그)는 의미가 모호하므로 그대로 두되, 문장 끝 단독 태그 나열은 제거를 검토한다.
- 주석 `%% ... %%`(Obsidian 전용 주석)는 제거한다.
- 코드블록, 표, 일반 마크다운 문법은 그대로 유지한다.

## 변환 시 주의

- **원본을 절대 변형하지 않는다.** 항상 `staging/`에 새 파일로 출력한다.
- 한 번에 여러 노트를 변환할 때는 파일별로 어떤 규칙이 적용됐는지 간단히 요약해 보고한다.
- 변환이 모호한 부분(끊긴 위키링크, 처리 보류한 이미지 등)은 변환 후 사용자에게 목록으로 알린다.
- 코드블록 **내부**의 텍스트는 변환 규칙을 적용하지 않는다 (예: 코드 안의 `[[...]]`는 그대로 둔다).
