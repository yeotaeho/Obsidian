프로젝트가 애초에 딥러닝을 사용할거라면 
처음에 바로 세팅cuda

natural language processing

NLP

# cuda

![[Pasted image 20251211124036.png]]
![[Pasted image 20251211124105.png]]

https://parksrazor.tistory.com/786

![[Pasted image 20251211124513.png]]

일순위는 버전체크이다.

일단 python == 3.8 버전 부터 체크한다.

현재 텐서플로우 파이토치 공식 홈페이지에서 말하는 필요한 cuda와 cudnn의 버전은 윈도우 11 버전에 맞게 설치 전략을 알려줘 -- 지피티한테 물어봐


CUDA Toolkit은 설치하지 말고 , 환경: RTX 3050, Windows 11, Python 3.13, CUDA 12.4 버전 , conda 최신 프레임워크: PyTorch만 사용하고 , 텐서플로를 설치하지 않음

으로 진행하면 



![[Pasted image 20251211145105.png]]
클릭
없으면 설치 ㄱ



![[Pasted image 20251211145124.png]]

홈에서 드라이버로 이동하고 다운로드 버튼클릭

![[Pasted image 20251211145512.png]]



![[Pasted image 20251211145705.png]]
다운로드하면 설치까지


![[Pasted image 20251211145726.png]]

계속

![[Pasted image 20251211150515.png]]
설치 완료 했으면 

컴퓨터 재부팅 껏다키셈

그다음 터미널에서 
![[Pasted image 20251211151010.png]]

conda create -n torch313 python=3.13 치기
python=3.13 뒤에 3.13은 자기 파이썬 버전에 맞춰서 하면좋음 크게는 상관없음


![[Pasted image 20251211151100.png]]
하면 밑에 y or n하는데 y로

![[Pasted image 20251211151312.png]]
했으면 

![[Pasted image 20251211151619.png]]

conda init하고

conda activate torch313치기
![[Pasted image 20251211151822.png]]


다음에 
PyTorch (CUDA 12.4 포함 휠) 설치
휠만 설치 
그전에 pip최신 버전으로 업그

![[Pasted image 20251211153440.png]]
pip install --upgrade pip




후에 https://pytorch.org/
사이트 들어가서
![[Pasted image 20251211153758.png]]

get started 클릭

![[Pasted image 20251211154010.png]]
위 이미지에 버전이랑 이것저것 다 체크해서
Run this Command 부분에 cli을
pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126

가져다 쓰면됨
터미널에 pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126 치면됨
![[Pasted image 20251211154223.png]]


![[Pasted image 20251211154932.png]]
하면 python입력 후


import torch


if torch.cuda.is_available():
    print("GPU count:", torch.cuda.device_count())
    print("Current device index:", torch.cuda.current_device())
    print("Current device name:", torch.cuda.get_device_name(0))
else:
    print("CUDA is NOT available.")

입력
![[Pasted image 20251211161502.png]]
이렇게 나오면 정상
설치 끝




다음
cuda 설치후 추가하는 라이브러리

transformers, sentencepiece, torchsummary, konlpy, opencv-python, nlti , wordcloud , jtpe
이 라이브러리을 자연어 처리(NLP)을 취해 설치 하는거임

진행중인 프로젝트마다 설치 해줘야함 

transformers, sentencepiece, torchsummary, konlpy, opencv-python, nlti , wordcloud , jtpe 이것을 (torch313) PS C:\Users\hi\Documents\learn_2> 여기에 설치하는데, cuda 12.6을 기준으로 최적의 버전은 너가 알아서 설치해줘 설치해줘

![[Pasted image 20251211161858.png]]

설치 완료됐다고 하면
 conda list을 쳐보면 
 ![[Pasted image 20251211164009.png]]
 이런식으로 나와야한다 여기에 설치한 라이브러리가 있는지 확인 해보고 없으면 커서한테 따져
 torch313 환경에 설치 한 것 인지 왜 안나오는지 등 
 만약 커서가  torch313 환경이 활설화되어있지 않다고 하면 다시한번 검토해보라고 


https://wikidocs.net/book/2155
딥 러닝을 이용한 자연어 처리


# 텍스트 전처리

**전처리확장**

텍스트 전처리 

코퍼스 -> 정제 -> 정규화 -> 토큰화 

코퍼스 , 토큰

코퍼스,토큰을 ds,dt으로 개념을 이해하면 될듯?
ds,dt은 머신러닝에서 썻었고
코퍼스,토큰은 딥러인에서만 처리할수있음


코퍼스는 간단히 말해서 텍스트 데이터의 집합 즉 비정형 데이터
여기서 텍스트 데이터의 집합의 범위는 
![[Pasted image 20251212095901.png]]

토큰
여기서에 토큰은 텍스트 데이터의 
![[Pasted image 20251212100019.png]]


![[Pasted image 20251212095726.png]]


# 정제 및 정규화

![[Pasted image 20251212100252.png]]


![[Pasted image 20251212100353.png]]

![[Pasted image 20251212100423.png]]

![[Pasted image 20251212100431.png]]

# 어간추출, 표제어추출
![[Pasted image 20251212100900.png]]


**어간추출(stemming)**
![[Pasted image 20251212101250.png]]


**표제어추출(lemmatization)**
![[Pasted image 20251212101226.png]]

# 표제어 , 형태소

![[Pasted image 20251212101448.png]]

![[Pasted image 20251212101511.png]]

# 불용어(stopword)

![[Pasted image 20251212102702.png]]

![[Pasted image 20251212102723.png]]

|개념|설명|
|---|---|
|불용어|의미가 약하고 자주 등장해서 제거하는 단어|
|목적|의미 있는 단어에 집중 + 모델 성능/속도 향상|
|예시|은/는/이/가, the, a, of 등|
|주의|항상 제거하는 건 아님 (not 등 중요 단어 존재)|


# 정규표현식

![[Pasted image 20251212103727.png]]
`?교` - 학교
`*교` - 
`+교` - 
`교^`


![[Pasted image 20251212104323.png]]



![[Pasted image 20251212104704.png]]


**패딩**

원핫인코딩

bag of words(bow)



삼성보고서에 대한 자연어 처리에 대한 코드 

`import pandas as pd
import nltk
from nltk.tokenize import sent_tokenize, word_tokenize, RegexpTokenizer
from nltk.stem import PorterStemmer, LancasterStemmer, WordNetLemmatizer
from nltk.tag import pos_tag, untag
from nltk import Text, FreqDist
from wordcloud import WordCloud
import matplotlib.pyplot as plt
import os
from pathlib import Path
from datetime import datetime
from konlpy.tag import Okt
import logging
import re
  
logger = logging.getLogger(__name__)
  
class SamsungReport:

  

    """
    삼성전자 리포트 분석 서비스 클래스
    """

    def __init__(self):
        self.okt = Okt()
    def text_process(self):
        self.find_freq()
        self.draw_wordcloud()
        return {
            '전처리 결과' : '완료',
            'freq_txt' : freq_txt,
        }

  

    def read_file(self):
        self.okt.pos("삼성전자 글로벌센터 전자사업부부" , stem=True)
        fname = '../data/kr-Report_2018.txt'
        with open(fname, 'r', encoding='utf-8') as f:
            text = f.read()
        return text

  

    def extract_hangeul(self, text:str):
        temp = text.replace('\n', '')
        tokenizer = re.compile(r'[^ ㄱ-힣]+')
        return tokenizer.sub('', temp)
    def change_token(self, texts):
        return word_tokenize(texts)

  

    def extract_noun(self):
        # 삼성전자의 스마트폰은 -> 삼성전자 스마트폰
        noun_tokens = []
        tokens = self.change_token(self.extract_hangeul(self.read_file()))
        for i in tokens:
            pos = self.okt.pos(i)
            temp = [j[0] for j in pos if j[1] == 'Noun']
            if len(''.join(temp)) > 1 :
                noun_tokens.append(''.join(temp))
        texts = ' '.join(noun_tokens)
        logger.info(texts[:100])
        return texts


    def read_stopword(self):
        self.okt.pos("삼성전자 글로벌센터 전자사업부", stem=True)
        fname = '../data/stopwords.txt'
        with open(fname, 'r', encoding='utf-8') as f:
            stopwords = f.read()
        return stopwords

  

    def remove_stopword(self):
        texts = self.extract_noun()
        tokens = self.change_token(texts)
        # print('------- 1 명사 -------')
        # print(texts[:30])
        stopwords = self.read_stopword()
        # print('------- 2 스톱 -------')
        # print(stopwords[:30])
        # print('------- 3 필터 -------')
        texts = [text for text in tokens
                 if text not in stopwords]
        # print(texts[:30])
        return texts

  
    def find_freq(self):
        texts = self.remove_stopword()
        freqtxt = pd.Series(dict(FreqDist(texts))).sort_values(ascending=False)
        logger.info(freqtxt[:30])
        return freqtxt

    def draw_wordcloud(self, filename: str = None, save_path: str = None, show: bool = True):
        """
        워드클라우드 생성 및 저장
        Args:
            filename: 저장할 파일명 (기본값: None, 자동 생성)
            save_path: 저장할 폴더 경로 (기본값: None, app/nlp/save 사용)
            show: 그래프 표시 여부
        Returns:
            저장된 파일 경로
        """
        texts = self.remove_stopword()
        # WordCloud 생성
        wcloud = WordCloud(
            width=1000,
            height=600,
            relative_scaling=0.2,
            background_color='white'
        ).generate(" ".join(texts))

        # 저장 경로 설정
        if save_path is None:
            # 현재 파일의 위치를 기준으로 save 폴더 찾기
            current_file = Path(__file__).resolve()
            save_dir = current_file.parent.parent / "save"
        else:
            save_dir = Path(save_path)
        # save 폴더가 없으면 생성
        save_dir.mkdir(parents=True, exist_ok=True)
        # 파일명 생성
        if filename is None:
            timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
            filename = f"samsung_wordcloud_{timestamp}.png"
        elif not filename.endswith('.png'):
            filename = f"{filename}.png"
        # 전체 파일 경로
        filepath = save_dir / filename
        # 이미지 저장
        wcloud.to_file(str(filepath))
        logger.info(f"워드클라우드가 저장되었습니다: {filepath}")
        if show:
            plt.figure(figsize=(12, 12))
            plt.imshow(wcloud, interpolation='bilinear')
            plt.axis('off')
            plt.show()
        return str(filepath)`





# 퍼셉트론

딥러닝은 퍼셉트론에 집합이다
뉴런 - 퍼셉트론

nn

딥러닝에 핵심기능 

X = {
    "review": "난이거아직도생각하면슬퍼서운다고..",
    "date": "05.10.15",
    "rating": "10",
    "author": "kiss****",
    "review_id": "443170",
    "movie_id": "10940"
  }

이렇게 정형화된 데이터가

X = [0.12, 3.5, 0, 10, ...]
이런식으로 기계가 알아먹을수있도록 
전처리되고 

x의 각 숫자마다 가중치을 부여 처음에 램덤값(거의 의미가없는 값)으로 가중치을 부여하면
y즉 라벨(정답)을 기준으로 비교 해서 오차을 계산(Loss)하고나면 가중치값을 수정 

**딥러닝**에서는 y까지 가기전에 필터들을 걷치고 감
![[Pasted image 20251212174824.png]]
이 이미지처럼 수많은 필터(층)을 즉 각 필터(층)을 지날때마다 가중치을 수정최종적으로 y에 도달하면 
y에서 라벨(정답)을 기준으로 오차을 계산(Loss)하여 가중치을 수정하고 역전파(다시 수정된 가중치값으로 처음부터다시) 이런과정을 반복해서 예측한 y값과 실제y(정답)이 맞으면 끝
정리
오차가 있으면 역전파로 수정된 가중치값으로 한번더 돌게됨 그렇게 예측된 y값이 실제y값과 비교해서 오차가있으면 가중치을 수정하고 또다시 정답이랑 맞으면 중지

**y = wx+b**


**머신 러닝**에서는 역전파 개념이 없음 필터(층)같은 개념도 없음 단층구조

대신 머신러닝에서는 로지스틱 회귀, svm,결정트리을 이용해 가중치 수정

![[Pasted image 20251212154721.png]]

인공신경망


**피드 포워드 신경망 언어 모델(Neural Network Language Model)NNLM**
피드 포워드 신경망은 입력의 길이가 고정되어 있어 자연어 처리를 위한 신경망으로는 한계가 있음 <--이거안쓸거임

그 이후에 나온 RNNLM도 있는데 이것도 안쓸거임 한계가 있음





스퀀스

dense
sparse

활성화 함수

![[Pasted image 20251212160337.png]]
![[Pasted image 20251212160443.png]]



KoELECTRA-Small-v3



에포크
몇번 학습과정을 실시 할지 
5에포크면 5번


**word2vec**
![[Pasted image 20251215141538.png]]
![[Pasted image 20251215141752.png]]

CBOW(Continuous Bag of Word)
단어뒤에 무슨 단어가 나올지 연관성 예을들어 고양이가 ~한다

어텐션
모델이 입력 데이터의 “어디에 집중해야 하는지”를 스스로 결정하는 메커니즘
중요한 정보에 가중치을 주는 메커니즘

단어들을 벡터화 시킬려면 하나로 쭉나열 리스트구조?

bert

# 모델
nn -> ann -> dnn(딥러닝 은닉층이 존재하는 신경망?) - >cnn,rnn

메커니즘 , 데이터의 조합이 모델

알고리즘은 있어도 되고 없어도되고 여러개잇어도되고 
알고리즘은 학습할방법 이라생각하면됨 설계도 같은 개념

하지만 알고리즘이 없으면 학습을 할수가없음 학습하기위해서는 알고리즘이 필수 
그렇게 학습한 모델을 배포할때는 알고리즘을 제거하고 배포
![[Pasted image 20251216102835.png]]

데이터는 말그래도 학습자료같은 것 
 
메커니즘은 기본적인 기능이라생각 하면됨 손가락 움직이기
알고리즘을 바탕으로 움직이는 기초적인 구조? 기능? 라고생각하면됨

![[Pasted image 20251216095346.png]]


모델은 또한 데이터을 가지고 정해진 길에따라 왔다갔다 하는애 이건 자바에서나 파이썬에서나 같은 기능 
대신 자바에서는 무지성으로 왔다갔다 했지만 파이썬모델은 배우면서 왔다갔다함
![[Pasted image 20251218114123.png]]
처음에 라우터 -> 서비스 -> 레파지토리 이런순으로 경로을 정해주고 이동하면서 학습함 
![[Pasted image 20251218114323.png]]

![[Pasted image 20251218115549.png]]
chat.py는 일반적으로 사용자가 채팅을 입력하면 출력하는 코드가 실행되는 라우터
rag_router.py는 temper
traning_router.py는 사용자가 채팅을 치면 입력한 글에따라 ai가 학습하는 코드을 실행하는 라우터 추가로 DB에 저장까지
![[Pasted image 20251218115908.png]]
chat_service.py는 chat.py라우터에 연동되어서 사용자가 채팅을 입력하면 맞게 출력하게 
embedding_ingest_service는 traning_router.py가 실행하면 실행되는
외부에서 추가되는 데이터 ingest

![[Pasted image 20251218120230.png]]


AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig


# 파운데이션 모델,베이스 모델

![[Pasted image 20251218165639.png]]

라마 GPT

![[Pasted image 20251229095954.png]]
라마란 범용적인 대형 모델에서 
LLaMA-3-8B
LLaMA-3-70B
이렇게 한가지에 특화된 대규모 학습이끝난 실제로 가져다 쓸 출발점이 되는 모델

| 구분  | 파운데이션 모델   | 베이스 모델       |
| --- | ---------- | ------------ |
| 성격  | 개념 / 분류    | 실제 사용 모델     |
| 범위  | 매우 넓음      | 구체적          |
| 크기  | 보통 매우 큼    | 8B, 70B 등 다양 |
| 목적  | 범용 AI 기반   | 작업 시작점       |
| 예시  | LLaMA, GPT | LLaMA-3-8B   |

|비유|의미|
|---|---|
|자동차 플랫폼|파운데이션 모델|
|소나타 / 그랜저|베이스 모델|
|택시·경찰차 개조|파인튜닝 모델|
# 트랜스포머
대표적인LLM기본구조 아키텍처?

트랜스포머는 제일 기본적인 llm의 핵심 기본구조(아키텍처)
![[Pasted image 20251216140525.png]]
![[Pasted image 20251216140537.png]]

| 개념             | 예시               |
| -------------- | ---------------- |
| 엔진 작동 원리(메커니즘) | Attention        |
| 차체 설계(아키텍처)    | Transformer      |
| 실제 자동차(모델)     | GPT, BERT, LLaMA |
| 대형 트럭          | LLM              |




# 랭체인(langchain)

https://www.langchain.com/
설치
sing up 하고
![[Pasted image 20251216144419.png]]
에서 
![[Pasted image 20251216144426.png]]
클릭

![[Pasted image 20251216144452.png]]

에서 키는 생성하면되고 밑에 cli는 cmd창에 치면됨 토치켜서

https://github.com/langchain-ai/langchain
여기 깃주소 클론해서 
받아오고 커서로 들어가서 현재 langchain구조을 fastAPI구조로 봐꿔줘 라고 프롬프트
모델이 학습하고 가져올려면 fastAPI와 같은구조에서 해야함 


기존lm들을 모은것이 llm이라 부름
lm까진 fastapi에서 커버가능 llm으로 넘어가선 불가능
그래서 랭체인 같은 llm전용 프레임워크가 나옴 이곳에서 벡터 db랑 데이터 체인이랑
뭐 RAG같은걸 구현

스프링에서 파이프라고 부르는것을 여기서는 체인이라 부름
결국 구조은 같은데 불려지는 이름만 다른거라 생각하면됨

llm전용 학습공간이라 생각하면됨 

invoke
batch

stream

# RAG


RAG(Retrieval-Augmented Generation)
![[Pasted image 20251216125218.png]]

여기서에 retrieval는 진짜 리트리버을 모방으로 리트리버가 공을 물고오는 것을 비유해 
사용자가 어떤 질문을 하면 그질문에 의미적으로 관련있는 정보을 물어오는것을 의미함
정보을 물어오는곳이 DB 외부데이터들

이전에는 일반적인 serch검색 도서관에서 제목으로 책 찾기 같은 거였다면
이젠 retreval 사서에게 “이런 내용의 책”이라고 말하면 추천받기임

GPT - Generative Pre-trained Transformer
생성형 언어가 학습된 트랜스포머
이런 GPT같은 모델을 만들기 위한 설계도가 RAG 아키턱처라고 보면됨
![[Pasted image 20251216143942.png]]
즉 모델 과 연결 뭐 GPT모델과도 LLaMA와도 연결에 대한 개념임
**RAG은 체인 즉 파이프라이다**

![[Pasted image 20251216144106.png]]


1. 사용자 질문 입력
2. retriever가 pgvector에서 유사한 문서 3개 검색
3. 검색된 문서를 "문맥"으로 만듦
4. 프롬프트 템플릿에 "문맥 + 질문" 결합
5. ChatGPT(gpt-3.5-turbo)에 전달
6. GPT가 문맥 기반으로 답변 생성
7. 문자열로 파싱하여 반환


[프론트엔드 (Next.js)]
        ↓ HTTP POST /api/chat
        ↓ {"message": "벡터 검색이란?"}
        ↓
[api_server.py]
        ↓
[get_vectorstore()]
  - OpenAI 임베딩 생성
  - RAG_DOCUMENTS를 pgvector에 저장 (최초 1회)
        ↓
[create_rag_chain()]
  - Retriever 생성
  - LLM (gpt-3.5-turbo) 준비
  - 체인 구성
        ↓
[rag_chain.invoke()]
  1. "벡터 검색이란?" → OpenAI로 벡터화
  2. pgvector에서 유사 문서 3개 검색
     → "벡터 검색은 의미 기반..."
     → "pgvector는 PostgreSQL..."
     → "LangChain은 LLM..."
  3. 문맥 + 질문을 GPT에 전달
  4. GPT 응답: "벡터 검색은..."
        ↓
[ChatResponse]
        ↓ {"response": "벡터 검색은..."}
        ↓
[프론트엔드에 표시]




**벡터db**
정형화되지 않은것을 저장하는곳 예들들어 pdf파일이나 hwp파일등 
정형화해서 저장되는것이 아니라 그냥 비정형화 그대로 저장됨

FAISS DB
CHROMA DB
PGVECTOR DB
이것말고도 많음
각각에 프로젝트에 맞는 db을 물어봐서 찾아


LLaMA

# peft(파인튜닝)


![[Pasted image 20251217111252.png]]

여기서 파라미터는
y = wx + b
에 w와b값을 파라미터라고함 즉 가중치와 bias의 영향을 주는
w,b의 일부만 학습시키는
학습하는 파라미터 수을 줄이는것

좀더 쉽게 말하면 
어댑터 패턴

모델에 각 목적에맞는 어댑터을 결합 쉽게 말해서
220v에 전류을 110v에 어댑터을 이용해서 110v 만 쓰는?
모델에 연결해서 데이터을 어느 부분만 주는? 튜닝할 목적에 맞는 데이터을 주는

이렇게 peft는 튜닝하는데 있어서 전략(개념)이고
여기서 실제로 구현할 구체적인 방법(알고리즘)은 LoRA다 

# LoRA,QLoRA

이렇게 peft는 튜닝하는데 있어서 전략(개념)이고
여기서 실제로 구현할 구체적인 방법(알고리즘)은 LoRA다 

로라
LoRA 풀 파인 튜닝
LoRA + 양자화 =  QLoRA

![[Pasted image 20251217120941.png]]

![[Pasted image 20251217121006.png]]

위 이미지 에서 가중치 행렬이 나왔는데 
x = [x1, x2, x3, x4]

W = [ 0.12 0.33 0.22 0.2123 ]
이렇게 실수인 가중치가 있으면 실수 가중치를 “아주 적은 비트(4bit)”로 표현하는 것
- **실수 → 정수 인덱스**
    
- 하지만 **0/1이 아니라 0~15 (16단계)**

![[Pasted image 20251218104037.png]]


**AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig**

## rank


**batch_size**
60000개의 데이터가 있으면 한번에 몇개식 가져와서 학습 뭐 100개식 
![[Pasted image 20251224115544.png]]

**shuffle**

![[Pasted image 20251224115807.png]]

![[Pasted image 20251224120052.png]]

| 항목     | shuffle=True | shuffle=False |
| ------ | ------------ | ------------- |
| 학습 안정성 | 높음           | 낮음            |
| 수렴 속도  | 빠름           | 느림            |
| 일반화    | 좋음           | 나쁠 수 있음       |
| 재현성    | 낮음           | 높음            |
![[Pasted image 20251224120137.png]]


딥러닝 필터
스파스는 구멍이 큼 그러니까 정확도는 떨어지고 빠름

덴스 구멍이 미세함 그러니까 정확하고 느림

yann lecun

yoshua bengio