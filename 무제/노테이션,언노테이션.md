
### 1. **노테이션(Annotation, 흔히 “어노테이션”이라고도 부름)**

- **정의**: 자바에서 코드에 메타데이터(추가 정보를 제공)로 붙이는 특별한 표시입니다.
    
- **역할**: 코드의 의미를 보강하거나, 컴파일러/런타임에 특정 동작을 지시할 때 사용합니다.
    
- **문법**: `@` 기호를 사용
    
- **예시**:
    

`@Override public String toString() {     return "Hello"; }  @Entity public class User {     @Id     private Long id; }`

- `@Override` → 부모 클래스의 메서드를 오버라이드하고 있음을 알려줌.
    
- `@Entity`, `@Id` → 스프링이나 JPA에서 DB 테이블과 매핑할 때 사용.
- 
![[Pasted image 20251030102143.png]]

여기서 ![[Pasted image 20251030102201.png]]
이부분이 언노테이션


![[Pasted image 20251030102228.png]]
이부분이 노테이션

노테이션은 “표현 방식” 자체를 의미함
언노테이션은 “정보를 덧붙이는 주석이나 태그”

![[Pasted image 20251030102143.png]]
노테이션에 언노테이션이 붙여져있는 것을 스프링에서 빈(bean) 이라고 한다



![[Pasted image 20251030105901.png]]
언노테이션 실행 방식 controller ps = new productscontroller 로 해서 
![[Pasted image 20251030105923.png]]
에 생성함 즉 new 없이 controller을 쓸수있는 이유는 @Controller가 자체적으로 controller ps = new productscontroller이런 식으로 application(최상위 부모객체)에 생성함 이렇게 언노테이션으로 application에 생성한 객체(싱글톤 객체)는 지워지지않는다 인스턴스가 아니다

언노테이션으로 생성한 객체는 싱글톤 객체

자바랑 데이터베이스랑 연결 할때도 언노테이션을 이용할거임 지워지지않은 객체이 때문 스프링을 이용해서 만들거임


**싱글톤**
어떤 클래스의 인스턴스가 **오직 하나만 생성**되도록 보장하는 디자인 패턴이에요.

그리고 그 인스턴스를 **전역적으로 접근할 수 있도록** 제공합니다.

![[Pasted image 20251103172015.png]]

언노테이션 
둘다 생성자 언노테이션
@NoArgsConstructor빈깡통 class pickproentity () 값을 넣을수있음 
@AllArgsConstructor처음부터 값이 들어가있는 깡통 class pickproentity (indexId) 값이 정해져 있는 상태로 수정불가 
지금 class pickproentity () 과 class pickproentity (indexId) 이것이 


@CrossOrigin 
origin을 서로 교차 시킨다 연결한다 여기서 origin은 url로 출처라고 생각하면됨

![[Pasted image 20251111102741.png]]
이렇게 하면 프론트에서 axios을 통해 값을 보내면 백에서 crossorigin을 통해 값을 받는다


스프링이 직접 생성해서 컨테이너에 등록한 객체만 스프링 빈이다.

![[Pasted image 20251117103152.png]]

“스프링 컨텍스트는 스프링 빈을 만들고 관리하는 큰 환경(저장소)이며, 그 안에 스프링이 직접 생성하여 등록한 객체만 스프링 빈이다.”


컨텍스트
- **Java/Spring**: `ApplicationContext` → 스프링이 관리하는 **빈(bean)들의 집합과 설정 정보**
    
- **JPA**: `Persistence Context` → **엔티티 객체들을 관리하고, DB와 동기화하는 환경**
ㅇ유레카도 있음

@Component 스프링에서 자동으로 @controller @service @repositroy 이것들을 생성

컨->서->레->엔

@Component의 자식들
@controller
@service
@repositroy
@E
이런 언노테이션이 연결하는 언노테이션?



@bean
Config연결