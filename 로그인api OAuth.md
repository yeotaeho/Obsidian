
카카오 
네이버
구글

![[Pasted image 20251126110925.png]]

1. 사용자가 "카카오톡 계정으로 로그인" 클릭
2. 프론트엔드 → GET /api/user/kakao/login
3. 백엔드 → 카카오 인증 URL 반환
4. 프론트엔드 → 카카오 로그인 페이지로 리다이렉트
5. 사용자가 카카오 계정으로 로그인
6. 카카오 → http://localhost:3000/auth/kakao/callback?code=xxx 로 리다이렉트
7. 프론트엔드 콜백 페이지 → POST /api/user/kakao/callback (code 전달)
8. 백엔드 → 카카오 토큰 발급 + 사용자 정보 조회 → 응답
9. 프론트엔드 → 로그인 처리 후 대시보드로 이동

![[Pasted image 20251128095220.png]]
5번에서 사용자가 카카오서버에서 로그인을 함으로서 인증이 된다 그로인해 권한을 부여 해주는 코드을 즉 인가 코드을 준다

![[Pasted image 20251128095228.png]]
그리고 마지막으로 
- 백엔드는 10번에서 받은 사용자 정보를 확인합니다.
    
- 우리 서비스의 DB에 해당 유저가 있는지 확인(회원가입/로그인 처리)합니다.
    
- 우리 서비스 전용 **JWT(AccessToken, RefreshToken)**를 생성합니다.
    
- 이 JWT를 클라이언트에게 전달하며 "이제 우리 서비스 로그인이 완료되었다"라고 알려줍니다.

여기서 우리 서비스 전용 JWT(AccessToken, RefreshToken)를 생성할때 
![[Pasted image 20260107143244.png]]
이러한 규칙들을 설정




지금까지 나눈 핵심 내용들을 바탕으로, **보안과 성능**이라는 두 마리 토끼를 잡기 위해 왜 지금의 구조가 탄생했는지 흐름별로 정리해 드릴게요.

---

## 1. JWT를 백엔드(서버)에서만 저장할 때의 문제점

서버가 모든 사용자의 로그인 상태를 메모리나 DB에 들고 있는 방식(Stateful)은 다음과 같은 한계가 있습니다.

- **서버 자원 고갈:** 접속자가 많아질수록 서버 메모리 점유율이 올라가고 DB 조회 부하가 커집니다.
    
- **확장성 저해:** 서버를 여러 대로 늘릴 경우, 1번 서버에서 로그인한 유저 정보를 2번 서버가 알지 못해 별도의 공유 저장소(Redis 등)를 구축해야 하는 번거로움이 생깁니다.
    
- **Stateless 철학 위반:** 원래 JWT는 서버가 유저를 기억하지 않기 위해 만든 것인데, 서버가 저장하는 순간 그 장점이 사라집니다.
    


즉 사용자 즉 브라우저(httponly)에 리플레쉬 토큰을 저장하는 방식이 아닌 서버가 모든 사용자 정보을 저장하게 되면 우리 서비스에 사용자가 맞는지 일일이 확인해야함 

프론트와 분담할시 음 분담이라긴 보단 리플레쉬 토큰을 브라우저(httponly) 와 같은 쿠키에 저장하면 억세스만료 될때마다 서버에 요청을 할 필요없이 쿠키한테서 갱신하면 된다 하지만 

백에서도 리플레쉬 토큰을 저장해야하는데 사용자가 로그아웃이나 해킹 위협이 있을경우 
서버내에서 리플레쉬 토큰을 지워버림 결국 재 로그인 할때 백에서 토큰이 없으니 인증이 안됌



---

## 2. 프론트와 분담 시 해결되는 점 (Stateful → Stateless)

프론트엔드에 토큰을 넘겨주면 서버는 '기억'의 의무에서 벗어나 '판독'의 역할만 수행하게 됩니다.

- **서버 부하 감소:** 서버는 유저 정보를 들고 있지 않습니다. 유저가 보낸 토큰이 "우리 서버에서 발행한 게 맞는지" 수학적으로 검증만 하면 끝납니다.
    
- **무한한 확장성:** 어떤 서버로 요청이 가더라도 같은 `Secret Key`만 있다면 즉시 인증이 가능합니다.
    
- **보안과 성능의 균형:** * **Access Token:** 프론트(메모리)에 두어 **빠른 서비스 이용** 제공.
    
    - **Refresh Token:** 브라우저(HttpOnly 쿠키)와 백엔드(DB/Redis)에 두어 **안전한 통제권** 확보.
        

---

## 3. 전체 인증 흐름 (Final Flow)

전체 과정은 다음과 같은 유기적인 흐름으로 완성됩니다.

### ① 최초 로그인 (인증 단계)

1. 사용자가 로그인을 시도(OAuth 등)하면 백엔드는 DB를 확인합니다.
    
2. 백엔드는 **Access Token(AT)**과 **Refresh Token(RT)**을 생성합니다.
    
3. **RT**는 백엔드 DB(또는 Redis)에 저장해둡니다. (나중에 통제하기 위함)
    
4. 프론트엔드로 두 토큰을 보냅니다. (RT는 `HttpOnly` 쿠키로, AT는 JSON 응답으로)
    

### ② 서비스 이용 (인가 단계)

1. 프론트는 **AT**를 Zustand 같은 메모리에 보관하며, API를 보낼 때마다 헤더에 담아 보냅니다.
    
2. 서버는 DB를 뒤지지 않고, `Secret Key`로 **AT**의 서명만 확인하여 즉시 응답합니다.
    

### ③ 토큰 만료 및 갱신 (유지 단계)

1. **AT**가 만료되면 프론트가 갱신 요청을 보냅니다.
    
2. 이때 브라우저가 쿠키에 담긴 **RT**를 자동으로 서버에 전달합니다.
    
3. 서버는 자기 DB에 있는 **RT**와 비교해보고, 일치하면 새로운 **AT**를 발급합니다.
    

### ④ 로그아웃 (통제 단계)

1. 사용자가 로그아웃하면 백엔드는 **DB에 저장했던 RT를 삭제**합니다.
    
2. 이제 해커가 기존의 RT를 훔쳤더라도, 서버 장부에 없기 때문에 더 이상 AT를 재발급받을 수 없습니다.
    

---

정리하자면:

서버는 **"최초 확인"**과 **"최종 통제"**만 담당하고, **"일상적인 인증"**은 토큰을 가진 프론트엔드가 스스로 증명하게 함으로써 효율적인 시스템이 완성되는 것입니다.

이제 이 이론을 바탕으로 실제 구현(코드 작성) 단계로 넘어가 볼까요? 혹시 **프론트엔드에서 만료된 토큰을 감지하고 자동으로 재발급 요청을 보내는 로직(Axios Interceptor)**에 대해 먼저 알아보고 싶으신가요?


![[Pasted image 20260108094919.png]]




authentication 인증 리플래시토큰

authorization 권한  인가코드  액세스토큰

Redirect

토큰

pkce

# jwt
jwt 구조
![[Pasted image 20251126120027.png]]


머리 header
바디 Payload


디코딩 

인코딩

![[Pasted image 20251126115003.png]]

Payload 택배내용물
claims 송장

프론트

억세스토큰은 메모리 리플레쉬 토큰은 HttpOnly(쿠키)

억세스는 메모리(react state/모듈 변수)에만 보관
페이지 새로고침하면 사라짐

리플레쉬토큰 HttpOnly + Secure + SameSite 쿠키로 보관
js로 읽을수잇음

서버

억세스 레디스 업스테이쉬

리플레쉬 neon db



![[Pasted image 20260109181054.png]]

![[Pasted image 20260109181125.png]]

![[Pasted image 20260109181136.png]]