
온톨로지(모델 구조)
모든 것을 통합한 하나의 일관된 관점 구조? 스토어같은?

![[Pasted image 20260108124843.png]]
저장소 벡터db같은 여기서 mcp가 데이터을 에이전트로 보냄냄

에이전트가 특정데이터을 찾는데 도와주는 역활을 함 에이전트가 db에 들어가서 일일이 찾는것이 아닌 온톨로지가 알맞는 컬럼 스키마 값을 줌

# 토폴로지


# MCP(model context Protocol)
사용하기 전에는 하나하나 따로 연결해 줘야했다면 mcp는 멀티탭 같은 플러그만 꽂으면 바로 통신됨

mcp는 표준규격 
선으로 보기에는 애매함

또한 tool calling와 tool서버을 제공 즉 llm모델이 자동으로 툴을 선택
![[Pasted image 20260108130628.png]]


![[Pasted image 20260108123905.png]]
![[Pasted image 20260108124415.png]]

| **구분** | **MCP (Model Context Protocol)** | **JSON-RPC**             |
| ------ | -------------------------------- | ------------------------ |
| **정체** | AI와 외부 환경을 잇는 **인프라 표준**         | 데이터를 주고받는 **메시지 규격**     |
| **역할** | 서버-클라이언트 수명 주기 관리, 도구 발견         | 함수 호출 요청 및 결과값 전달        |
| **비유** | **전화국과 통신망 전체**                  | 전화기 너머로 주고받는 **한국어나 영어** |
| **관계** | **상위 개념 (프레임워크)**                | **하위 수단 (프로토콜)**         |
![[Pasted image 20260108130956.png]]

mcp는 서버와 모델들 등을 연결시킬수 있는 길에 json-rpc 통신규약으로 데이터(온톨로지)와 기능들 가져오게n8n(툴) 하는 결론적으로 지능형 중재자 역활을 함
![[Pasted image 20260108130813.png]]
![[Pasted image 20260108145020.png]]

### 노드
![[Pasted image 20260119114132.png]]
- **노드(Node):** "누가?" (에이전트 A, DB 서버 B, API Gateway C)

### 엣지
![[Pasted image 20260119114049.png]]
- **엣지(Edge):** "어떻게 연결되었나?" (A가 B를 호출함, B가 C에 데이터를 전송함)


프로시저

agent - to - agent(a2a)
data ontology

배치시스템

# devops - aiops
http(프로토콜) - mcp
서비스 - 에이전트
db - 온톨로지

devops는 파이브라인 즉 컨서레엔이 1차원 구조로 하나의 데이터을 일일이 처리하는 방면
aiops는 스트림 같은 데이터흐름으로는 에이전트한데 충분한 데이터을 공급못함 flow같은 데이터 흐름으로 데이터 하나하나 일일이 보내는 것이 아닌 일괄적으로 보내게 해야함
예을 들면 배달에서 주문자 4명이 주문을 보내면 배달기사 하나가 음식들을 다 전달 <- 이게 flow
배달기사 4명을 보내서 1대1로 하면 스트림


`langchain`(스트림) , `langgraph`(플로우) ->프레임워크(aiops프레임워크) 

엔터프라이즈

job
에이전트


워크플로우 - 설계도

webhook

![[Pasted image 20260119113749.png]]
![[Pasted image 20260119113755.png]]


![[Pasted image 20260119115145.png]]

규칙기반 기존 스프링 서비스
정책기반 에이전트 

게이트웨이에서 사용자에 요청이 정책기반에 대한 요청인지 
정책기반에 대한 요청인지 파악 후 정책기반이면 오케스트레이터 전달 하고 오케스트레이터가 알맞는 라우터 역활을 수행 
규칙기반이면 
일반 서비스 실행
# 에이전트

llm과 상호작용 하며 **루프(자기가 사는환경)** 하에서 목표을 위해 **도구(서로 연결하는 선)**를 호출하는 최소한의 단위

llm(속성)과 도구(기능)에 집합을 에이전트

사용자를 위해 독립적으로 일하는 ai시스템

`langchain`(스트림) , `langgraph`(플로우)


에이전트한테 툴로 라이브러리을 부여할수잇음
![[Pasted image 20260129154815.png]]

또한 시스템을 자동화 즉 에이전트가 관리하게 하기위해서는 백터db을 에이전트가 생성하고 기존의 postgredb에 임베딩 또한 에이전트가 하게 설정해둬야함 그렇기 때문에 에이전트한테 라이브러리등을 줘야함 <-- 학습으로 안한다는거임 로봇에 칩을 넣듯이 그렇게 작동함
기존의 있던 테이블을 백터로 자기가 만들게 해야함 
터미널에서 에이전트에게 자신에게 맞는 백터db을 만들라고 시키는 cli을 쳐야함
임베딩또한 

즉 컨택스트을 줘야함 엑사원에게 그래야지 자동화가 됨
https://gemini.google.com/share/883d0d888e42



머신러닝에서는 학습
모델을 가져와서 학습시키는것을 파인튜닝한다 라고 생각하면됨



# 루즈 커플링, 하드 커플링, 하이 Cohesion

MCP 등 연결할때 

![[Pasted image 20260130150427.png]]

| **구분**     | **하드 커플링 (강한 결합)**     | **루즈 커플링 (약한 결합)**  |
| ---------- | ---------------------- | ------------------- |
| **의존성**    | 매우 높음 (상대방의 내부를 알아야 함) | 낮음 (표준 인터페이스만 알면 됨) |
| **변경 영향**  | 도미노 현상처럼 전체에 영향을 줌     | 해당 모듈 내에서 영향이 국한됨   |
| **유지보수**   | 어려움 (코드 분석에 시간 소요)     | 쉬움 (모듈별 독립적 관리 가능)  |
| **MCP 관점** | 특정 서버 전용 클라이언트 코드 작성   | **MCP 표준 프로토콜 준수**  |


![[Pasted image 20260130150529.png]]



### 상태관리모델
# Pydantic 모델


판독- 결정 규칙

판별- 분류 정책

에이전트끼리 역활을 판독과 판별로 놔누어서

예을들어 KoELECTRA이 스팸메일일 확률 알려주고 EXAONE이 최종결정 이런식으로 진행


https://brunch.co.kr/@vsongyev/28



 DDD DEA



허브, 스포크  - 스타구조

리프

휴리스틱 기반으로 정책 규칙인지 판단
휴리스틱이란 규칙인지 정책인지 판단할 필요가 없을때?





![[Pasted image 20260122103022.png]]



# 웹 3.0

시맨틱 웹 - gpt

![[Pasted image 20260129124531.png]]

# ReAct패턴


MCP 라이브러리 중에서 
fastMCP(프레임워크) 쓸거임

MCP 을 추상으로 fastMCP을 구현
MCP는 타입같은존재


| **개념**            | **비유 (웹 개발)**           | **비유 (AI 도구 개발)**         |
| ----------------- | ----------------------- | ------------------------- |
| **규약 (Protocol)** | **HTTP** (데이터를 주고받는 규칙) | **MCP** (모델과 도구가 대화하는 규칙) |
| **도구 (Library)**  | **FastAPI / Flask**     | **fastMCP**               |
| **결과물 (Server)**  | 웹 API 서버                | MCP 서버 (도구 모음)            |




리듀스


bullmq