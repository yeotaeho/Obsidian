
처음 프로젝트 만들때 이구조로 만들것 
이구조로 만들면 즉 마이크로서비스로 하면 
서비스 하나하나 분리 할수있음 이게 큰 장점임
어느 한 서비스에서 에러가 나면 그것만 중지 하면됨 기존에는 그게 안됐음 


큰구조는 작은구조와 같다 이런 패턴이 잇었던거같은데

로드밸런서 
라운드 로빈 방식으로 리액트와 스프링을 연결 시켜주는 중재자 

마이크로서비스 서비스을 쪼갬 각각에 서비스 하나 고장 나면 그것만 중지 시키면됨
모놀리식 서비스덩어리 하나 고장나면 다 고장

엔트리포인트-게이트 웨이 - 엣지서버(경계선)

프론트에서 게이트웨이 까지 가는것을 http라고하고 게이트웨이 에서 컨트롤러 까지 가는 것을 라우팅이라함

gateway는 통합적인것 다 gateway configuration도 게이트 웨이  eurekae

discovery라우터

gateway->secrets->configuration->eureka

gateway->secrets->configuration->eureka->discovery

gateway->configuration->eureka


eureka검색 디스커버리 찾아줌
configuration 구성저장소

![[Pasted image 20251111125015.png]]

![[Pasted image 20251111125035.png]]

마이크로서비스

![[Pasted image 20251112090954.png]]


![[Pasted image 20251112091022.png]]
위 사진 처럼 configuration 폴더뿐만 아니라 api-server의 하위폴더 들은 dockerflie(확정자 없음)을 넣고 
api에는 docker-compose.yaml을 생성한다 
api폴더에 있는 docker-compose.yaml을 실행 하면 각각에 하위폴더들의 도커가 활성화 되야함 
![[Pasted image 20251112091724.png]]
위 사진에 처럼 각각에 도커에 코드을 작성해줘야함 9라인에 entrypoint은 자기 폴더이름을 적어줘야함 "/eureka-server.jar"

docker-compose.yaml은 ![[Pasted image 20251112092005.png]]

이런식으로 만들어주는데 그냥 커서한테 docker-compose.yaml파일을 주고 각각폴더에 있는 
dockerflie을 연결시켜줘 라고 하면 만들어줌

각각에 build gradle들로 봐꿔 줘야함 
dependencies {
    implementation group: 'http://org.springframework.cloud', name: 'spring-cloud-config-server', version: '4.1.3'
    implementation group: 'org.springframework.boot', name: 'spring-boot-starter-actuator', version: '3.3.4'
    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
    implementation 'http://org.springframework.cloud:spring-cloud-config-server'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'
}

이처럼 dependencies안에 내용을 봐꿔줘야하는데 이건 프로그램에 맞춰서 해야함..



# 키설정

yml에
![[Pasted image 20251111163052.png]]
현재사진은 신형키로 함

SSH key 생성 (이메일은 본인 GitHub 이메일로)
ssh-keygen -t rsa -b 4096 -C "tkakrrl1234@gmail.com"구형 키

ssh-keygen -t ed25519 -C "tkakrrl1234@gmail.com"신형 키


# 파일 저장 위치 물어보면 Enter (기본: ~/.ssh/id_rsa)
# passphrase 설정 (선택사항, 보안을 위해 설정 권장)


![[Pasted image 20251111162204.png]]

생성 완료

![[Pasted image 20251111162242.png]]
들어가면
![[Pasted image 20251111162304.png]]
.ssh 폴더가 생긴다 들어가서 
![[Pasted image 20251111162337.png]]
위에가 개인키
밑에가 공개키다

깃에 키 등록 
git에 등록 할 키는 공개키로 등록

![[Pasted image 20251111164514.png]]

![[Pasted image 20251111164526.png]]


키설정후
![[Pasted image 20251112093219.png]]
.env을 만들고 그 안에 이렇게 키을 복사후 붙여놓고 
![[Pasted image 20251112093318.png]]


configuration > application 에 키을 사진 처럼 봐꿔줌
![[Pasted image 20251112093310.png]]

![[Pasted image 20251112093605.png]]
api 폴더안에 gitignore파일 맨밑에 .env을 추가 해줌

![[Pasted image 20251112093719.png]]
secrets 안에 configuration을 제외한 각각폴더에 yaml을 넣는다 내용을 안같아도됨 test만 일단
![[Pasted image 20251112093836.png]]



서브모듈화 
이렇게 구조을 만들면 모듈화을 시켜줘야함 그래야지 aop 이상적인 그림이 나옴

![[Pasted image 20251113091701.png]]
이렇게 하위 폴더들을 만들어 줘야함
![[Pasted image 20251113091751.png]]
이렇게 구조을 만들어주고

![[Pasted image 20251113091801.png]]
도커 파일까지


이렇게 모듈화까지 했으면 `docker compose up` 을 했을때 

![[Pasted image 20251114164027.png]]

![[Pasted image 20251114164035.png]]

이렇게 도커 이미지와 컨테이너가 생성및 실행되야함 
실행되면 db까지
![[Pasted image 20251114164443.png]]
이렇ㄱ게

![[Pasted image 20251114164140.png]]

유레카 화면까지 떠야하는데 각 yaml을 수정해 줘야함
![[Pasted image 20251114164350.png]]

이런식으로 



원래 react에서 중재자 후 게이트웨이 스프링가고 DB로 대충 이런식으로 갔는데

ai가 나오면서 유레카 게이트웨이에 추가 될 게 생겻다 spring cloud gateway webflux